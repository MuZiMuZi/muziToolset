<!-- AUTO-GENERATED BY scripts/generate_mkdocs_reference.py -->

# `muziToolset.systems.body.skirt.builder`

> 本页由 `scripts/generate_mkdocs_reference.py` 根据当前源码 AST 自动生成。
> 源码 Docstring 是参数、返回值、异常和示例说明的第一事实来源。

## 概览

**用途**

程序化裙子绑定系统。

**模块定位**

System 业务构建层；负责可重复构建的完整 Rig Component 或 Workflow。
## 常用任务

- 构建可以被多个工具复用的完整 Rig Component / Workflow 时。
- 需要稳定输入、输出和可重复 Build 行为时。

## Import

```python
from muziToolset.systems.body.skirt import builder
```

## API 一览

### Class `SkirtRigBuilder`

裙子绑定系统 Builder。

| Method | 作用 |
| --- | --- |
| `validate_parameters(self)` | 检查 Builder 参数。 |
| `get_names(self)` | 返回系统内所有固定节点名称。 |
| `ensure_root_groups(self)` | 确保裙子系统基础层级存在。 |
| `create_setup(self)` | 创建或重建裙子定位系统。 |
| `select_setup_curves(self)` | 选择当前裙子系统的两条定位曲线。 |
| `build(self)` | 根据当前 Blueprint 创建完整裙子 FK 绑定。 |

## Classes 详细 API

### `SkirtRigBuilder`

裙子绑定系统 Builder。

#### `__init__()`

**作用**

初始化裙子绑定系统参数。

**Signature**

```python
__init__(self, name='skirt', horizontal_count=8, vertical_count=4)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `name` | `str` | 否 | `'skirt'` | 创建或查询时使用的节点名称。 |
| `horizontal_count` | `int` | 否 | `8` | 当前构建、采样或查询过程使用的元素数量。 |
| `vertical_count` | `int` | 否 | `4` | 当前构建、采样或查询过程使用的元素数量。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.systems.body.skirt import builder

instance = builder.SkirtRigBuilder()
```

#### `validate_parameters()`

**作用**

检查 Builder 参数。

**Signature**

```python
validate_parameters(self)
```

**参数**

无。

**返回值**

bool:
    当前操作成功或目标状态满足要求时返回 True，否则返回 False。

**异常**

- `ValueError`：输入数据、场景状态或操作条件不满足要求时抛出。

**示例**

```python
from muziToolset.systems.body.skirt import builder

instance = builder.SkirtRigBuilder()

result = instance.validate_parameters()
```

#### `get_names()`

**作用**

返回系统内所有固定节点名称。

**Signature**

```python
get_names(self)
```

**参数**

无。

**返回值**

dict:
    包含本次构建、查询或处理结果的结构化字典。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.systems.body.skirt import builder

instance = builder.SkirtRigBuilder()

result = instance.get_names()
```

#### `ensure_root_groups()`

**作用**

确保裙子系统基础层级存在。

**Signature**

```python
ensure_root_groups(self)
```

**参数**

无。

**返回值**

object:
    当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.systems.body.skirt import builder

instance = builder.SkirtRigBuilder()

result = instance.ensure_root_groups()
```

#### `create_setup()`

**作用**

创建或重建裙子定位系统。

**Signature**

```python
create_setup(self)
```

**参数**

无。

**返回值**

dict:
    包含本次构建、查询或处理结果的结构化字典。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.systems.body.skirt import builder

instance = builder.SkirtRigBuilder()

result = instance.create_setup()
```

#### `select_setup_curves()`

**作用**

选择当前裙子系统的两条定位曲线。

**Signature**

```python
select_setup_curves(self)
```

**参数**

无。

**返回值**

object | list:
    按当前 API 约定顺序返回的结果列表。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.systems.body.skirt import builder

instance = builder.SkirtRigBuilder()

result = instance.select_setup_curves()
```

#### `build()`

**作用**

根据当前 Blueprint 创建完整裙子 FK 绑定。

**Signature**

```python
build(self)
```

**参数**

无。

**返回值**

dict:
    包含本次构建、查询或处理结果的结构化字典。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.systems.body.skirt import builder

instance = builder.SkirtRigBuilder()

result = instance.build()
```

## 源码位置

`systems/body/skirt/builder.py`
