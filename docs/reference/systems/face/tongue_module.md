<!-- AUTO-GENERATED BY scripts/generate_mkdocs_reference.py -->

# `muziToolset.systems.face.tongue_module`

> 本页由 `scripts/generate_mkdocs_reference.py` 根据当前源码 AST 自动生成。
> 源码 Docstring 是参数、返回值、异常和示例说明的第一事实来源。

## 概览

**用途**

TongueModule：舌头五段 FK 绑定模块。

**模块定位**

System 业务构建层；负责可重复构建的完整 Rig Component 或 Workflow。
## 常用任务

- 构建可以被多个工具复用的完整 Rig Component / Workflow 时。
- 需要稳定输入、输出和可重复 Build 行为时。
- 构建或管理 Face Setup、Guide、Eyelid、Lip 等面部绑定流程时。

## Import

```python
from muziToolset.systems.face import tongue_module
```

## API 一览

### Class `TongueModule`

使用标准五段 FKChain 实现的舌头绑定模块。

## Classes 详细 API

### `TongueModule`

使用标准五段 FKChain 实现的舌头绑定模块。

#### `__init__()`

**作用**

初始化 TongueModule。

TongueModule 只保存舌头自己的固定业务配置，真正的 Joint、Controller、
Constraint 创建和删除全部由 FKChain 统一处理。

**Signature**

```python
__init__(self, module='tongue', side='md', guide=None, jnt_parent=None, ctrl_parent=None, ctrl_axis='X+')
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `module` | `str` | 否 | `'tongue'` | 模块名称，正式舌头模块默认使用 "tongue"。 |
| `side` | `str` | 否 | `'md'` | 舌头通常位于中线，默认使用 "md"。 |
| `guide` | `list[str] \| tuple[str] \| None` | 否 | `None` | Guide Template 导入完成后提供的五个 Tongue Locator，顺序必须和 FK 链一致。 |
| `jnt_parent` | `str \| None` | 否 | `None` | Tongue Joint 总组需要挂接的上层节点。 |
| `ctrl_parent` | `str \| None` | 否 | `None` | Tongue Controller 总组需要挂接的上层节点。 |
| `ctrl_axis` | `str` | 否 | `'X+'` | Controller Shape 轴向。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.systems.face import tongue_module

instance = tongue_module.TongueModule()
```

## 源码位置

`systems/face/tongue_module.py`
