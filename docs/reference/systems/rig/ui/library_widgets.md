<!-- AUTO-GENERATED BY scripts/generate_mkdocs_reference.py -->

# `muziToolset.systems.rig.ui.library_widgets`

> 本页由 `scripts/generate_mkdocs_reference.py` 根据当前源码 AST 自动生成。
> 源码 Docstring 是参数、返回值、异常和示例说明的第一事实来源。

## 概览

**用途**

绑定库的轻量绘制组件。 矢量装饰随窗口缩放，不依赖外部图片或字体文件。

**模块定位**

System 业务构建层；负责可重复构建的完整 Rig Component 或 Workflow。
## 常用任务

- 构建可以被多个工具复用的完整 Rig Component / Workflow 时。
- 需要稳定输入、输出和可重复 Build 行为时。

## Import

```python
from muziToolset.systems.rig.ui import library_widgets
```

## API 一览

### Functions

| API | 作用 |
| --- | --- |
| `module_icon(color, code='')` | 绘制统一的线框模块图标。 |

### Class `ArtHeader`

页眉中的工业线条、斜切面与酸橙色装饰。

| Method | 作用 |
| --- | --- |
| `paintEvent(self, event)` | 绘制不参与交互的轻量背景装饰。 |

### Class `StepButton`

顶部四步导航，以大数字、切角和酸橙强调显示工作流状态。

| Method | 作用 |
| --- | --- |
| `set_stage_state(self, state)` | 设置 available / current / completed / locked 视觉状态。 |
| `paintEvent(self, event)` | 绘制步骤卡片，并保持 QPushButton 的交互状态。 |

### Class `TickBox`

为酸橙色选中框补充明确勾号，同时保留 Qt 键盘与辅助功能。

| Method | 作用 |
| --- | --- |
| `paintEvent(self, event)` | 在 Qt 默认 CheckBox 之上绘制更清晰的勾号。 |

### Class `Section`

可折叠属性抽屉，折叠时释放垂直空间。

| Method | 作用 |
| --- | --- |
| `set_expanded(self, expanded)` | 同步标题箭头和属性区可见性。 |

## 公共常量

| 名称 | 值 |
| --- | --- |
| `INK` | `'#232a27'` |
| `MUTED` | `'#8a9089'` |
| `LINE` | `'#d9ddd5'` |
| `PAPER` | `'#fbfbf7'` |
| `LIME` | `'#d7ef49'` |
| `LIME_SOFT` | `'#edf6bd'` |
| `LIME_DARK` | `'#617126'` |

## Functions 详细 API

### `module_icon()`

**作用**

绘制统一的线框模块图标。

图标保持低饱和线框风格；模块列表的真正强调色由选中状态负责，避免
每个模块同时使用高饱和颜色导致视觉层级混乱。

**Signature**

```python
module_icon(color, code='')
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `color` | `str` | 是 | `—` | 模块提供的基础颜色。仅作为轻微识别色使用。 |
| `code` | `str` | 否 | `''` | 保留给后续模块类型图形区分使用。 |

**返回值**

QIcon:
    40 x 40 的矢量风格图标。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.systems.rig.ui import library_widgets

result = library_widgets.module_icon(
    color=...,
)
```

## Classes 详细 API

### `ArtHeader`

页眉中的工业线条、斜切面与酸橙色装饰。

#### `paintEvent()`

**作用**

绘制不参与交互的轻量背景装饰。

**Signature**

```python
paintEvent(self, event)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `event` | `QtCore.QEvent \| object` | 是 | `—` | Qt Event 回调传入的事件对象。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.systems.rig.ui import library_widgets

instance = library_widgets.ArtHeader()

result = instance.paintEvent(
    event=...,
)
```

### `StepButton`

顶部四步导航，以大数字、切角和酸橙强调显示工作流状态。

#### `__init__()`

**作用**

初始化步骤按钮。

**Signature**

```python
__init__(self, number, title, subtitle, parent=None)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `number` | `int` | 是 | `—` | 自动编号或字母编号转换使用的序号。 |
| `title` | `str` | 是 | `—` | 窗口、Section、Dialog 或报告使用的标题文本。 |
| `subtitle` | `object` | 是 | `—` | 当前方法执行 Maya / Rig 操作时使用的 `subtitle` 数据。 |
| `parent` | `str` | 否 | `None` | 父级 Maya 节点名称。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.systems.rig.ui import library_widgets

instance = library_widgets.StepButton(
    number=1,
    title=...,
    subtitle=...,
)
```

#### `set_stage_state()`

**作用**

设置 available / current / completed / locked 视觉状态。

**Signature**

```python
set_stage_state(self, state)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `state` | `object` | 是 | `—` | 当前方法执行 Maya / Rig 操作时使用的 `state` 数据。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.systems.rig.ui import library_widgets

instance = library_widgets.StepButton(
    number=1,
    title=...,
    subtitle=...,
)

result = instance.set_stage_state(
    state=...,
)
```

#### `paintEvent()`

**作用**

绘制步骤卡片，并保持 QPushButton 的交互状态。

**Signature**

```python
paintEvent(self, event)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `event` | `QtCore.QEvent \| object` | 是 | `—` | Qt Event 回调传入的事件对象。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.systems.rig.ui import library_widgets

instance = library_widgets.StepButton(
    number=1,
    title=...,
    subtitle=...,
)

result = instance.paintEvent(
    event=...,
)
```

### `TickBox`

为酸橙色选中框补充明确勾号，同时保留 Qt 键盘与辅助功能。

#### `paintEvent()`

**作用**

在 Qt 默认 CheckBox 之上绘制更清晰的勾号。

**Signature**

```python
paintEvent(self, event)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `event` | `QtCore.QEvent \| object` | 是 | `—` | Qt Event 回调传入的事件对象。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.systems.rig.ui import library_widgets

instance = library_widgets.TickBox()

result = instance.paintEvent(
    event=...,
)
```

### `Section`

可折叠属性抽屉，折叠时释放垂直空间。

#### `__init__()`

**作用**

初始化 Section 标题按钮与 Form Body。

**Signature**

```python
__init__(self, title, parent=None)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `title` | `str` | 是 | `—` | 窗口、Section、Dialog 或报告使用的标题文本。 |
| `parent` | `str` | 否 | `None` | 父级 Maya 节点名称。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.systems.rig.ui import library_widgets

instance = library_widgets.Section(
    title=...,
)
```

#### `set_expanded()`

**作用**

同步标题箭头和属性区可见性。

**Signature**

```python
set_expanded(self, expanded)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `expanded` | `object` | 是 | `—` | 当前方法执行 Maya / Rig 操作时使用的 `expanded` 数据。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.systems.rig.ui import library_widgets

instance = library_widgets.Section(
    title=...,
)

result = instance.set_expanded(
    expanded=...,
)
```

## 源码位置

`systems/rig/ui/library_widgets.py`
