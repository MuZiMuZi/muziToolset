<!-- AUTO-GENERATED BY scripts/generate_mkdocs_reference.py -->

# `muziToolset.systems.rig.ui`

> 本页由 `scripts/generate_mkdocs_reference.py` 根据当前源码 AST 自动生成。
> 源码 Docstring 是参数、返回值、异常和示例说明的第一事实来源。

## 概览

**用途**

Modular Rig UI Package。

**模块定位**

System 业务构建层；负责可重复构建的完整 Rig Component 或 Workflow。
## 常用任务

- 构建可以被多个工具复用的完整 Rig Component / Workflow 时。
- 需要稳定输入、输出和可重复 Build 行为时。

## Import

```python
from muziToolset.systems.rig import ui
```

## API 一览

### Functions

| API | 作用 |
| --- | --- |
| `create_window()` | 创建 Modular Rig Window。 |

## Functions 详细 API

### `create_window()`

**作用**

创建 Modular Rig Window。

**Signature**

```python
create_window()
```

**参数**

无。

**返回值**

object:
    创建或构建完成后的 Maya / Rig 对象或 Build Result。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.systems.rig import ui

result = ui.create_window()
```

## 源码位置

`systems/rig/ui/__init__.py`
