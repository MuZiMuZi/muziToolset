<!-- AUTO-GENERATED BY scripts/generate_mkdocs_reference.py -->

# `muziToolset.ui.widgets.color_index_slider`

> 本页由 `scripts/generate_mkdocs_reference.py` 根据当前源码 AST 自动生成。
> 源码 Docstring 是参数、返回值、异常和示例说明的第一事实来源。

## 概览

**用途**

可复用的 Maya Index Color 选择控件。

**模块定位**

UI 公共层；负责可复用界面组件，不承载 Rig 算法。
## 常用任务

- 多个工具需要复用统一的 PySide 窗口、控件或主题样式时。

## Import

```python
from muziToolset.ui.widgets import color_index_slider
```

## API 一览

### Class `MayaIndexColorSlider`

Maya Index Color 滑条、数值标签和颜色预览组合控件。

| Method | 作用 |
| --- | --- |
| `style_widgets(self)` | 应用高可见度 Slider、Index Label 和 Preview 样式。 |
| `get_value(self)` | 返回当前 Maya Index Color。 |
| `set_value(self, value)` | 设置当前 Maya Index Color，并自动限制到 0～31。 |
| `update_preview(self, color_index=None)` | 根据 Maya Index Color 更新数值标签和颜色方块预览。 |

## Classes 详细 API

### `MayaIndexColorSlider`

Maya Index Color 滑条、数值标签和颜色预览组合控件。

#### `__init__()`

**作用**

初始化 Maya Index Color Slider。

**Signature**

```python
__init__(self, value=17, parent=None)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `value` | `int` | 否 | `17` | 初始 Maya Index Color，最终会限制在 0～31。 |
| `parent` | `QtWidgets.QWidget \| None` | 否 | `None` | 可选 Qt 父控件。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.ui.widgets import color_index_slider

instance = color_index_slider.MayaIndexColorSlider()
```

#### `style_widgets()`

**作用**

应用高可见度 Slider、Index Label 和 Preview 样式。

**Signature**

```python
style_widgets(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.ui.widgets import color_index_slider

instance = color_index_slider.MayaIndexColorSlider()

result = instance.style_widgets()
```

#### `get_value()`

**作用**

返回当前 Maya Index Color。

**Signature**

```python
get_value(self)
```

**参数**

无。

**返回值**

int:
    Slider 当前 0～31 的颜色索引。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.ui.widgets import color_index_slider

instance = color_index_slider.MayaIndexColorSlider()

result = instance.get_value()
```

#### `set_value()`

**作用**

设置当前 Maya Index Color，并自动限制到 0～31。

**Signature**

```python
set_value(self, value)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `value` | `int` | 是 | `—` | 需要写入的 Maya Index Color。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.ui.widgets import color_index_slider

instance = color_index_slider.MayaIndexColorSlider()

result = instance.set_value(
    value=1,
)
```

#### `update_preview()`

**作用**

根据 Maya Index Color 更新数值标签和颜色方块预览。

**Signature**

```python
update_preview(self, color_index=None)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `color_index` | `int \| None` | 否 | `None` | 需要显示的 0～31 Maya Index；None 时读取 Slider 当前值。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.ui.widgets import color_index_slider

instance = color_index_slider.MayaIndexColorSlider()

result = instance.update_preview()
```

## 源码位置

`ui/widgets/color_index_slider.py`
