<!-- AUTO-GENERATED BY scripts/generate_mkdocs_reference.py -->

# `muziToolset.core.rigging.jnt_utils`

> 本页由 `scripts/generate_mkdocs_reference.py` 根据当前源码 AST 自动生成。
> 源码 Docstring 是参数、返回值、异常和示例说明的第一事实来源。

## 概览

**用途**

jnt_utils：Maya Joint 基础工具。

**模块定位**

Core 底层公共能力；不负责 UI 和完整 Rig Workflow。
## 常用任务

- 在多个 Tool / System 中复用同一套 Maya 底层能力时。
- 希望业务逻辑与 UI 分离，并保持可测试、可组合时。

## Import

```python
from muziToolset.core.rigging import jnt_utils
```

## API 一览

### Class `Jnt`

源码暂未提供类说明。

| Method | 作用 |
| --- | --- |
| `set_match_transform(self, target, position=True, rotation=True)` | 将当前 Joint 对齐到指定目标的位置和旋转。 |
| `set_radius(self, radius)` | 设置当前 Joint 的显示半径。 |
| `reset_joint_orient(self)` | 清除当前 Joint 的关节定向数值。 |

## Classes 详细 API

### `Jnt`

源码暂未提供类说明。

#### `__init__()`

**作用**

初始化 Joint 工具对象。

如果 Maya 场景中已经存在指定名称的 Joint，则直接将它作为当前 Joint。
如果不存在，则自动创建一个新的 Joint。
这样后续所有方法都可以直接使用 self.jnt，不需要重复判断 Joint 是否存在。
name(str): Joint 名称。
Maya 使用示例：
from muziToolset.core.rigging import jnt_utils
jnt_object = jnt_utils.Jnt("jnt_lf_arm_bind_001")
print(jnt_object.jnt)

**Signature**

```python
__init__(self, name)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `name` | `str` | 是 | `—` | 创建或查询时使用的节点名称。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.rigging import jnt_utils

instance = jnt_utils.Jnt(
    name="name",
)
```

#### `set_match_transform()`

**作用**

将当前 Joint 对齐到指定目标的位置和旋转。

target(str/PyNode): 需要对齐的目标对象，例如 Guide、Locator 或 Transform。

**Signature**

```python
set_match_transform(self, target, position=True, rotation=True)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `target` | `str` | 是 | `—` | 接收结果或被处理的目标 Maya 节点名称。 |
| `position` | `bool` | 否 | `True` | Jnt / Transform 使用的 XYZ Position。 |
| `rotation` | `bool` | 否 | `True` | Jnt / Transform 使用的 XYZ Rotation。 |

**返回值**

None

    Maya 使用示例：

    from muziToolset.core.rigging import jnt_utils

    jnt_object = jnt_utils.Jnt("jnt_lf_arm_bind_001")
    target = "guide_lf_arm_001"

    jnt_object.match_transform(target)

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.rigging import jnt_utils

instance = jnt_utils.Jnt(
    name="name",
)

result = instance.set_match_transform(
    target="target",
)
```

#### `set_radius()`

**作用**

设置当前 Joint 的显示半径。

radius(float): Joint 的显示半径数值。

**Signature**

```python
set_radius(self, radius)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `radius` | `float` | 是 | `—` | 创建节点或控制器使用的半径值。 |

**返回值**

None

    Maya 使用示例：

    from muziToolset.core.rigging import jnt_utils

    jnt_object = jnt_utils.Jnt("jnt_lf_arm_bind_001")
    radius = 0.5

    jnt_object.set_radius(radius)

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.rigging import jnt_utils

instance = jnt_utils.Jnt(
    name="name",
)

result = instance.set_radius(
    radius=1.0,
)
```

#### `reset_joint_orient()`

**作用**

清除当前 Joint 的关节定向数值。

将 jointOrientX、jointOrientY、jointOrientZ 一次性设置为 0。

**Signature**

```python
reset_joint_orient(self)
```

**参数**

无。

**返回值**

None

    Maya 使用示例：

    from muziToolset.core.rigging import jnt_utils

    jnt_object = jnt_utils.Jnt("jnt_lf_arm_bind_001")

    jnt_object.reset_joint_orient()

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.rigging import jnt_utils

instance = jnt_utils.Jnt(
    name="name",
)

result = instance.reset_joint_orient()
```

## 源码位置

`core/rigging/jnt_utils.py`
