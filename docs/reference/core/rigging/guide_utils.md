<!-- AUTO-GENERATED BY scripts/generate_mkdocs_reference.py -->

# `muziToolset.core.rigging.guide_utils`

> 本页由 `scripts/generate_mkdocs_reference.py` 根据当前源码 AST 自动生成。
> 源码 Docstring 是参数、返回值、异常和示例说明的第一事实来源。

## 概览

**用途**

guide_utils：Maya Module Guide 基础工具。

**模块定位**

Core 底层公共能力；不负责 UI 和完整 Rig Workflow。
## 常用任务

- 在多个 Tool / System 中复用同一套 Maya 底层能力时。
- 希望业务逻辑与 UI 分离，并保持可测试、可组合时。

## Import

```python
from muziToolset.core.rigging import guide_utils
```

## API 一览

### Class `Guide`

源码暂未提供类说明。

| Method | 作用 |
| --- | --- |
| `import_template(self)` | 导入当前 Module 的标准 Guide 模板，并返回 Guide Root。 |
| `get_module_group(self, module)` | 获取指定模块对应的 Guide Module Group。 |
| `get_guides(self, module, side=None)` | 获取指定模块组下面的 Locator Guide。 |
| `create_guide_curve(self, module, side)` | 为指定 Guide 模块和方向创建一条 Guide Display Curve。 |

## Classes 详细 API

### `Guide`

源码暂未提供类说明。

#### `__init__()`

**作用**

初始化 Module Guide 工具对象。

module(str): Guide 模块名称，默认 "face"。

**Signature**

```python
__init__(self, module='face')
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `module` | `str` | 否 | `'face'` | 当前 Maya / Rig 操作使用的 `module` 名称或标记。 |

**返回值**

None

    Maya 使用示例：

    from muziToolset.core.rigging import guide_utils

    guide_object = guide_utils.Guide("face")

    print(guide_object.guide_root_name)
    print(guide_object.guide_template_path)

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.rigging import guide_utils

instance = guide_utils.Guide()
```

#### `import_template()`

**作用**

导入当前 Module 的标准 Guide 模板，并返回 Guide Root。

Guide Display Curve 属于模板本身的一部分。
正常使用时只需要导入模板，不需要再额外调用 create_guide_curve()。

**Signature**

```python
import_template(self)
```

**参数**

无。

**返回值**

PyNode: 当前 Module Guide Root。

    Maya 使用示例：

    from muziToolset.core.rigging import guide_utils

    guide_object = guide_utils.Guide("face")
    guide_root = guide_object.import_template()

    print(guide_root)

**异常**

- `IOError`：输入数据、场景状态或操作条件不满足要求时抛出。
- `RuntimeError`：输入数据、场景状态或操作条件不满足要求时抛出。
- `TypeError`：输入数据、场景状态或操作条件不满足要求时抛出。

**示例**

```python
from muziToolset.core.rigging import guide_utils

instance = guide_utils.Guide()

result = instance.import_template()
```

#### `get_module_group()`

**作用**

获取指定模块对应的 Guide Module Group。

module 会按照统一命名规则转换成模块组名称：
    ear  -> grp_md_ear_guide_001
    eye  -> grp_md_eye_guide_001
    brow -> grp_md_brow_guide_001
方法会检查模块组是否存在、是否为 Transform，
并确认它属于当前 Guide Root。
module(str): 需要获取的 Guide 模块名称，例如 "ear"、"eye"、"brow"。

**Signature**

```python
get_module_group(self, module)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `module` | `object` | 是 | `—` | 当前方法执行 Maya / Rig 操作时使用的 `module` 数据。 |

**返回值**

PyNode: 找到的 Guide Module Group。
    None: 模块组不存在，或不属于当前 Guide Root。

    Maya 使用示例：

    from muziToolset.core.rigging import guide_utils

    guide_object = guide_utils.Guide("face")
    guide_object.import_template()

    ear_group = guide_object.get_module_group("ear")
    print(ear_group)

**异常**

- `TypeError`：输入数据、场景状态或操作条件不满足要求时抛出。

**示例**

```python
from muziToolset.core.rigging import guide_utils

instance = guide_utils.Guide()

result = instance.get_module_group(
    module=...,
)
```

#### `get_guides()`

**作用**

获取指定模块组下面的 Locator Guide。

Face Guide 模板作为整体导入，但真正读取和构建时按照面部模块分别处理。
side 不传时返回整个模块的 Locator；传入 side 后只返回对应方向的 Locator。
返回结果会按照 Guide 节点名称排序。
当前 Guide 序号统一使用 001、002、003 形式，因此可以稳定得到创建 Joint 所需的顺序。
module(str): 需要获取 Guide 的模块名称，例如 "ear"、"eye"、"brow"。
side(str): 可选方向，只接受 "lf"、"rt"、"md" 或 None。

**Signature**

```python
get_guides(self, module, side=None)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `module` | `object` | 是 | `—` | 当前方法执行 Maya / Rig 操作时使用的 `module` 数据。 |
| `side` | `str` | 否 | `None` | 方向标记，常用值为 lf、rt 或 md。 |

**返回值**

list: 符合 module 和 side 条件，并按名称排序后的 Locator Guide PyNode 列表。

    Maya 使用示例：

    from muziToolset.core.rigging import guide_utils

    guide_object = guide_utils.Guide("face")
    guide_object.import_template()

    ear_guides = guide_object.get_guides("ear")
    lf_ear_guides = guide_object.get_guides("ear", "lf")
    rt_ear_guides = guide_object.get_guides("ear", "rt")

    for guide in lf_ear_guides:
    print(guide)

**异常**

- `ValueError`：输入数据、场景状态或操作条件不满足要求时抛出。

**示例**

```python
from muziToolset.core.rigging import guide_utils

instance = guide_utils.Guide()

result = instance.get_guides(
    module=...,
)
```

#### `create_guide_curve()`

**作用**

为指定 Guide 模块和方向创建一条 Guide Display Curve。

这个方法主要用于制作或更新 Guide 模板。
正常绑定流程中，Display Curve 应该已经保存到 <module>_guide.ma 模板里，
import_template() 导入模板时会一起进入场景，不需要再次创建。
Locator Shape 的 worldPosition 直接连接 Curve Shape 的 controlPoints，
不额外创建 multMatrix 或 decomposeMatrix 节点。
module(str): 需要创建显示曲线的模块名称，例如 "ear"、"brow"。
side(str): Curve 方向，只接受 "lf"、"rt" 或 "md"。

**Signature**

```python
create_guide_curve(self, module, side)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `module` | `object` | 是 | `—` | 当前方法执行 Maya / Rig 操作时使用的 `module` 数据。 |
| `side` | `str` | 是 | `—` | 方向标记，常用值为 lf、rt 或 md。 |

**返回值**

PyNode: 创建或已经存在的 Guide Display Curve Transform。

    Maya 使用示例：

    from muziToolset.core.rigging import guide_utils

    guide_object = guide_utils.Guide("face")
    guide_object.import_template()

    lf_curve = guide_object.create_guide_curve("ear", "lf")
    rt_curve = guide_object.create_guide_curve("ear", "rt")

**异常**

- `ValueError`：输入数据、场景状态或操作条件不满足要求时抛出。
- `TypeError`：输入数据、场景状态或操作条件不满足要求时抛出。

**示例**

```python
from muziToolset.core.rigging import guide_utils

instance = guide_utils.Guide()

result = instance.create_guide_curve(
    module=...,
    side="side",
)
```

## 源码位置

`core/rigging/guide_utils.py`
