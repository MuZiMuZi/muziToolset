<!-- AUTO-GENERATED BY scripts/generate_mkdocs_reference.py -->

# `muziToolset.core.common.transform_utils`

> 本页由 `scripts/generate_mkdocs_reference.py` 根据当前源码 AST 自动生成。
> 源码 Docstring 是参数、返回值、异常和示例说明的第一事实来源。

## 概览

**用途**

transform_utils：Maya Transform 基础工具。

**模块定位**

Core 底层公共能力；不负责 UI 和完整 Rig Workflow。
## 常用任务

- 在多个 Tool / System 中复用同一套 Maya 底层能力时。
- 希望业务逻辑与 UI 分离，并保持可测试、可组合时。

## Import

```python
from muziToolset.core.common import transform_utils
```

## API 一览

### Class `Transform`

源码暂未提供类说明。

| Method | 作用 |
| --- | --- |
| `match_transform(self, target, position=True, rotation=True, scale=True)` | 将当前对象直接对齐到指定目标对象的 Transform。 |
| `get_world_matrix(self)` | 获取当前对象的世界矩阵。 |
| `set_world_matrix(self, matrix)` | 将给定的世界矩阵设置到当前对象。 |
| `reset_transform(self, translate=True, rotate=True, scale=True)` | 将当前对象的 Transform 数值恢复到默认状态。 |
| `get_transform(self)` | 获取当前对象对应的 Transform 节点。 |

## Classes 详细 API

### `Transform`

源码暂未提供类说明。

#### `__init__()`

**作用**

初始化 Transform 工具对象。

**Signature**

```python
__init__(self, object=None)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `object` | `str/PyNode` | 否 | `None` | 需要操作的 Maya 节点，可以是 Transform、Joint、Shape 等节点。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.common import transform_utils

instance = transform_utils.Transform()
```

#### `match_transform()`

**作用**

将当前对象直接对齐到指定目标对象的 Transform。

Guide 系统统一把 Locator Transform 作为真正的定位数据：
    Locator Translate / Rotate / Scale
                ↓
          Target Transform
不读取 Locator Shape.localPosition，也不额外计算 Shape.worldPosition。
这样 Joint、Controller 和其他绑定节点都使用同一套简单明确的对齐规则。

**Signature**

```python
match_transform(self, target, position=True, rotation=True, scale=True)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `target` | `str/PyNode` | 是 | `—` | 需要匹配的目标对象。 |
| `position` | `bool` | 否 | `True` | 是否匹配目标位置，默认 True。 |
| `rotation` | `bool` | 否 | `True` | 是否匹配目标旋转，默认 True。 |
| `scale` | `bool` | 否 | `True` | 是否匹配目标缩放，默认 True。 |

**返回值**

None

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.common import transform_utils

instance = transform_utils.Transform()

result = instance.match_transform(
    target="target",
)
```

#### `get_world_matrix()`

**作用**

获取当前对象的世界矩阵。

**Signature**

```python
get_world_matrix(self)
```

**参数**

无。

**返回值**

Matrix: 当前对象的世界矩阵。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.common import transform_utils

instance = transform_utils.Transform()

result = instance.get_world_matrix()
```

#### `set_world_matrix()`

**作用**

将给定的世界矩阵设置到当前对象。

**Signature**

```python
set_world_matrix(self, matrix)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `matrix` | `list[float] \| maya.api.OpenMaya.MMatrix` | 是 | `—` | 用于 Transform、Constraint 或空间计算的 4x4 Matrix 数据。 |

**返回值**

None

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.common import transform_utils

instance = transform_utils.Transform()

result = instance.set_world_matrix(
    matrix=1.0,
)
```

#### `reset_transform()`

**作用**

将当前对象的 Transform 数值恢复到默认状态。

**Signature**

```python
reset_transform(self, translate=True, rotate=True, scale=True)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `translate` | `bool` | 否 | `True` | 是否将 Translate 重置为 0。 |
| `rotate` | `bool` | 否 | `True` | 是否将 Rotate 重置为 0。 |
| `scale` | `bool` | 否 | `True` | 是否将 Scale 重置为 1。 |

**返回值**

None

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.common import transform_utils

instance = transform_utils.Transform()

result = instance.reset_transform()
```

#### `get_transform()`

**作用**

获取当前对象对应的 Transform 节点。

如果当前对象本身就是 Transform，则直接返回当前对象；
如果当前对象是 Shape，则返回它的父 Transform。

**Signature**

```python
get_transform(self)
```

**参数**

无。

**返回值**

PyNode: 当前对象对应的 Transform 节点。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.common import transform_utils

instance = transform_utils.Transform()

result = instance.get_transform()
```

## 源码位置

`core/common/transform_utils.py`
