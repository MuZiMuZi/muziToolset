<!-- AUTO-GENERATED BY scripts/generate_mkdocs_reference.py -->

# `muziToolset.core.bake.matehumanUtils`

> 本页由 `scripts/generate_mkdocs_reference.py` 根据当前源码 AST 自动生成。
> 源码 Docstring 是参数、返回值、异常和示例说明的第一事实来源。

## 概览

**用途**

matehumanUtils：这是一个用来对matehuman的基础功能定义的模块

**模块定位**

Core 底层公共能力；不负责 UI 和完整 Rig Workflow。
## 常用任务

- 在多个 Tool / System 中复用同一套 Maya 底层能力时。
- 希望业务逻辑与 UI 分离，并保持可测试、可组合时。

## Import

```python
from muziToolset.core.bake import matehumanUtils
```

## API 一览

### Class `MateHuman`

源码暂未提供类说明。

| Method | 作用 |
| --- | --- |
| `get_mateHuman_drv_jnt(description)` | 定义matehuman的骨架结构,查询到对应的模块后返回对应的关节 |
| `mateHuman_decompose(self)` | 拆分mateHuman的关节名称 |
| `export_face_animation()` | 导出面部的动画在文件的路径下 :return: |
| `export_body_animation()` | 导出身体的动画在文件的路径下 :return: |
| `reset_mateHuman_control()` | 重置控制器上所有的数值. |

## Classes 详细 API

### `MateHuman`

源码暂未提供类说明。

#### `__init__()`

**作用**

初始化当前对象，并准备运行时需要的状态和成员。

**Signature**

```python
__init__(self, name)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `name` | `str` | 是 | `—` | 创建或查询时使用的节点名称。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import matehumanUtils

instance = matehumanUtils.MateHuman(
    name="name",
)
```

#### `get_mateHuman_drv_jnt()`

**作用**

定义matehuman的骨架结构,查询到对应的模块后返回对应的关节

**Signature**

```python
get_mateHuman_drv_jnt(description)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `description` | `str` | 是 | `—` | UI Step / Section 中展示的功能说明文本。 |

**返回值**

object:
        当前查询匹配到的 Maya / Rig 数据；没有结果时按 API 约定返回空值。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import matehumanUtils

instance = matehumanUtils.MateHuman(
    name="name",
)

result = instance.get_mateHuman_drv_jnt(
    description=...,
)
```

#### `mateHuman_decompose()`

**作用**

拆分mateHuman的关节名称

**Signature**

```python
mateHuman_decompose(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import matehumanUtils

instance = matehumanUtils.MateHuman(
    name="name",
)

result = instance.mateHuman_decompose()
```

#### `export_face_animation()`

**作用**

导出面部的动画在文件的路径下 :return:

**Signature**

```python
export_face_animation()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import matehumanUtils

instance = matehumanUtils.MateHuman(
    name="name",
)

result = instance.export_face_animation()
```

#### `export_body_animation()`

**作用**

导出身体的动画在文件的路径下 :return:

**Signature**

```python
export_body_animation()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import matehumanUtils

instance = matehumanUtils.MateHuman(
    name="name",
)

result = instance.export_body_animation()
```

#### `reset_mateHuman_control()`

**作用**

重置控制器上所有的数值.

**Signature**

```python
reset_mateHuman_control()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import matehumanUtils

instance = matehumanUtils.MateHuman(
    name="name",
)

result = instance.reset_mateHuman_control()
```

## 源码位置

`core/bake/matehumanUtils.py`
