<!-- AUTO-GENERATED BY scripts/generate_mkdocs_reference.py -->

# `muziToolset.core.bake.vectorUtils`

> 本页由 `scripts/generate_mkdocs_reference.py` 根据当前源码 AST 自动生成。
> 源码 Docstring 是参数、返回值、异常和示例说明的第一事实来源。

## 概览

**用途**

当前模块尚未提供完整模块摘要。

**模块定位**

Core 底层公共能力；不负责 UI 和完整 Rig Workflow。
## 常用任务

- 在多个 Tool / System 中复用同一套 Maya 底层能力时。
- 希望业务逻辑与 UI 分离，并保持可测试、可组合时。

## Import

```python
from muziToolset.core.bake import vectorUtils
```

## API 一览

### Functions

| API | 作用 |
| --- | --- |
| `get_matrix(attr)` | 获取 Maya Matrix 属性并转换成 MMatrix。 |
| `matrix_to_list(matrix)` | 将 MMatrix 转换成 Maya setAttr 可以使用的 16 个浮点数。 |
| `matrix_parent_constraint(driver, driven, maintain_offset=True)` | 使用矩阵代替 parentConstraint。 |

### Class `Vector`

用于在maya中方便地计算vector3的数据类型类 初始化： 使用列表或元组：v = Vector([1, 2, 3])

| Method | 作用 |
| --- | --- |
| `normalize(self)` | 使长度的参数规范化，如果遇到除数为0的情况则pass |
| `length(self)` | sqrt是更号的意思，求长度 |
| `x(self)` | 执行当前 API 的主要处理流程。 |
| `y(self)` | 执行当前 API 的主要处理流程。 |
| `z(self)` | 执行当前 API 的主要处理流程。 |
| `as_list(self)` | 执行当前 API 的主要处理流程。 |
| `axis(self)` | 执行当前 API 的主要处理流程。 |
| `mult_interval(self, interval)` | 执行当前 API 的主要处理流程。 |

## Functions 详细 API

### `get_matrix()`

**作用**

获取 Maya Matrix 属性并转换成 MMatrix。

**Signature**

```python
get_matrix(attr)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `attr` | `str` | 是 | `—` | Matrix 属性名字。 例如： ctrl_l_arm.worldMatrix[0] jnt_l_arm.matrix |

**返回值**

maya.api.OpenMaya.MMatrix

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import vectorUtils

result = vectorUtils.get_matrix(
    attr="attr",
)
```

### `matrix_to_list()`

**作用**

将 MMatrix 转换成 Maya setAttr 可以使用的 16 个浮点数。

**Signature**

```python
matrix_to_list(matrix)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `matrix` | `maya.api.OpenMaya.MMatrix` | 是 | `—` | Maya API 2.0 的矩阵对象。 |

**返回值**

list:
    包含 16 个 float 的列表。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import vectorUtils

result = vectorUtils.matrix_to_list(
    matrix=...,
)
```

### `matrix_parent_constraint()`

**作用**

使用矩阵代替 parentConstraint。

**Signature**

```python
matrix_parent_constraint(driver, driven, maintain_offset=True)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `driver` | `str` | 是 | `—` | 驱动对象。 |
| `driven` | `str` | 是 | `—` | 被驱动对象。 |
| `maintain_offset` | `bool` | 否 | `True` | 是否保持当前偏移。 |

**返回值**

str:
    创建出来的 multMatrix 节点。

**异常**

源码未声明专门的异常说明。

**示例**

```python
matrix_parent_constraint(
            'ctrl_l_arm_fk',
            'jnt_l_arm_fk',
            maintain_offset=True
        )
```

## Classes 详细 API

### `Vector`

用于在maya中方便地计算vector3的数据类型类 初始化： 使用列表或元组：v = Vector([1, 2, 3])

使用参数列表（3）：v = Vector(1, 2, 3)

#### `__init__()`

**作用**

初始化当前对象，并准备运行时需要的状态和成员。

**Signature**

```python
__init__(self, *args)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `args` | `tuple` | 否 | `*args` | 当前方法按顺序处理的 `args` 数据集合。 |

**返回值**

源码未声明返回值说明。

**异常**

- `TypeError`：输入数据、场景状态或操作条件不满足要求时抛出。

**示例**

```python
from muziToolset.core.bake import vectorUtils

instance = vectorUtils.Vector()
```

#### `normalize()`

**作用**

使长度的参数规范化，如果遇到除数为0的情况则pass

**Signature**

```python
normalize(self)
```

**参数**

无。

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import vectorUtils

instance = vectorUtils.Vector()

result = instance.normalize()
```

#### `length()`

**作用**

sqrt是更号的意思，求长度

**Signature**

```python
length(self)
```

**参数**

无。

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import vectorUtils

instance = vectorUtils.Vector()

result = instance.length()
```

#### `x()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
x(self)
```

**参数**

无。

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import vectorUtils

instance = vectorUtils.Vector()

result = instance.x()
```

#### `y()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
y(self)
```

**参数**

无。

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import vectorUtils

instance = vectorUtils.Vector()

result = instance.y()
```

#### `z()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
z(self)
```

**参数**

无。

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import vectorUtils

instance = vectorUtils.Vector()

result = instance.z()
```

#### `as_list()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
as_list(self)
```

**参数**

无。

**返回值**

list:
        按当前 API 约定顺序返回的结果列表。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import vectorUtils

instance = vectorUtils.Vector()

result = instance.as_list()
```

#### `axis()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
axis(self)
```

**参数**

无。

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import vectorUtils

instance = vectorUtils.Vector()

result = instance.axis()
```

#### `mult_interval()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
mult_interval(self, interval)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `interval` | `object` | 是 | `—` | 当前方法执行 Maya / Rig 操作时使用的 `interval` 数据。 |

**返回值**

tuple:
        按当前 API 约定组织的结果元组。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import vectorUtils

instance = vectorUtils.Vector()

result = instance.mult_interval(
    interval=...,
)
```

## 源码位置

`core/bake/vectorUtils.py`
