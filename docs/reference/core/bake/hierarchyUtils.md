<!-- AUTO-GENERATED BY scripts/generate_mkdocs_reference.py -->

# `muziToolset.core.bake.hierarchyUtils`

> 本页由 `scripts/generate_mkdocs_reference.py` 根据当前源码 AST 自动生成。
> 源码 Docstring 是参数、返回值、异常和示例说明的第一事实来源。

## 概览

**用途**

当前模块尚未提供完整模块摘要。

**模块定位**

Core 底层公共能力；不负责 UI 和完整 Rig Workflow。
## 常用任务

- 在多个 Tool / System 中复用同一套 Maya 底层能力时。
- 希望业务逻辑与 UI 分离，并保持可测试、可组合时。

## Import

```python
from muziToolset.core.bake import hierarchyUtils
```

## API 一览

### Class `Hierarchy`

源码暂未提供类说明。

| Method | 作用 |
| --- | --- |
| `parent(child_node, parent_node)` | 先查找子物体和父物体之间是否有父子层级关系，没有的话制作父子层级关系 :param child_node（str）:子物体的节点名称 :param parent_nodestr）:父物体的节点名称 :return: |
| `add_extra_group(obj, grp_name, world_orient=False)` | 在对象上方添加一个额外的组. |
| `control_hierarchy()` | Add an upper level group to the controller. |
| `get_child_object(object, type='joint')` | 获取对象的所有子物体包括对象本身 :param object: 需要获取所有子物体的对象 type（str）:需要获取对象的类型 return: 所有子物体的名称列表 |
| `select_sub_objects(obj_type='transform')` | 快速选择所选择物体的所有子对象,将所有选择的对象名称返回出去方便其他函数调用 obj_type（type）:需要选择的物体的子对象的类型，比如'transform','joint' |
| `create_rig_grp()` | 创建绑定的默认层级组 |
| `create_default_grp()` | 添加绑定的初始层级组，并隐藏连接对应的属性 |
| `create_grp(grp, parent=None)` | 给定组的名称，查询给定的名称是否已经在场景里存在，没有的话则创建对应的组 |

## Classes 详细 API

### `Hierarchy`

源码暂未提供类说明。

#### `parent()`

**作用**

先查找子物体和父物体之间是否有父子层级关系，没有的话制作父子层级关系 :param child_node（str）:子物体的节点名称 :param parent_nodestr）:父物体的节点名称 :return:

**Signature**

```python
parent(child_node, parent_node)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `child_node` | `str` | 是 | `—` | 需要重新挂接父级的 Child DAG 节点名称。 |
| `parent_node` | `str` | 是 | `—` | Child 最终需要挂接到的 Parent DAG 节点名称。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import hierarchyUtils

instance = hierarchyUtils.Hierarchy()

result = instance.parent(
    child_node="child_node",
    parent_node="parent_node",
)
```

#### `add_extra_group()`

**作用**

在对象上方添加一个额外的组.

**Signature**

```python
add_extra_group(obj, grp_name, world_orient=False)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `obj` | `str` | 是 | `—` | 要添加额外组的Maya对象. |
| `grp_name` | `str` | 是 | `—` | 额外的组名 |
| `world_orient` | `bool` | 否 | `False` | 设置新组的世界位置是否改变。 |

**返回值**

str: 新添加的组.

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import hierarchyUtils

instance = hierarchyUtils.Hierarchy()

result = instance.add_extra_group(
    obj=...,
    grp_name="grp_name",
)
```

#### `control_hierarchy()`

**作用**

Add an upper level group to the controller.

The naming convention is
Type_Side_describe_index

**Signature**

```python
control_hierarchy()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import hierarchyUtils

instance = hierarchyUtils.Hierarchy()

result = instance.control_hierarchy()
```

#### `get_child_object()`

**作用**

获取对象的所有子物体包括对象本身 :param object: 需要获取所有子物体的对象 type（str）:需要获取对象的类型 return: 所有子物体的名称列表

**Signature**

```python
get_child_object(object, type='joint')
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `object` | `str` | 是 | `—` | 需要处理的 Maya 场景对象名称。 |
| `type` | `str` | 否 | `'joint'` | 当前 Maya / Rig 操作使用的 `type` 名称或标记。 |

**返回值**

object:
        当前查询匹配到的 Maya / Rig 数据；没有结果时按 API 约定返回空值。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import hierarchyUtils

instance = hierarchyUtils.Hierarchy()

result = instance.get_child_object(
    object="object",
)
```

#### `select_sub_objects()`

**作用**

快速选择所选择物体的所有子对象,将所有选择的对象名称返回出去方便其他函数调用 obj_type（type）:需要选择的物体的子对象的类型，比如'transform','joint'

**Signature**

```python
select_sub_objects(obj_type='transform')
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `obj_type` | `str` | 否 | `'transform'` | 当前 Maya / Rig 操作使用的 `obj_type` 名称或标记。 |

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import hierarchyUtils

instance = hierarchyUtils.Hierarchy()

result = instance.select_sub_objects()
```

#### `create_rig_grp()`

**作用**

创建绑定的默认层级组

**Signature**

```python
create_rig_grp()
```

**参数**

无。

**返回值**

tuple:
        按当前 API 约定组织的结果元组。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import hierarchyUtils

instance = hierarchyUtils.Hierarchy()

result = instance.create_rig_grp()
```

#### `create_default_grp()`

**作用**

添加绑定的初始层级组，并隐藏连接对应的属性

**Signature**

```python
create_default_grp()
```

**参数**

无。

**返回值**

dict:
        包含本次构建、查询或处理结果的结构化字典。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import hierarchyUtils

instance = hierarchyUtils.Hierarchy()

result = instance.create_default_grp()
```

#### `create_grp()`

**作用**

给定组的名称，查询给定的名称是否已经在场景里存在，没有的话则创建对应的组

**Signature**

```python
create_grp(grp, parent=None)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `grp` | `object` | 是 | `—` | 当前方法执行 Maya / Rig 操作时使用的 `grp` 数据。 |
| `parent` | `str` | 否 | `None` | 父级 Maya 节点名称。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import hierarchyUtils

instance = hierarchyUtils.Hierarchy()

result = instance.create_grp(
    grp=...,
)
```

## 源码位置

`core/bake/hierarchyUtils.py`
