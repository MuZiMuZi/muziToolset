<!-- AUTO-GENERATED BY scripts/generate_mkdocs_reference.py -->

# `muziToolset.core.bake.nameUtils`

> 本页由 `scripts/generate_mkdocs_reference.py` 根据当前源码 AST 自动生成。
> 源码 Docstring 是参数、返回值、异常和示例说明的第一事实来源。

## 概览

**用途**

nameUtils：Maya Rig 命名工具。

**模块定位**

Core 底层公共能力；不负责 UI 和完整 Rig Workflow。
## 常用任务

- 在多个 Tool / System 中复用同一套 Maya 底层能力时。
- 希望业务逻辑与 UI 分离，并保持可测试、可组合时。

## Import

```python
from muziToolset.core.bake import nameUtils
```

## API 一览

### Class `Name`

Rig 节点命名类。

| Method | 作用 |
| --- | --- |
| `type(self)` | 执行当前 API 的主要处理流程。 |
| `type(self, value)` | 执行当前 API 的主要处理流程。 |
| `side(self)` | 执行当前 API 的主要处理流程。 |
| `side(self, value)` | 执行当前 API 的主要处理流程。 |
| `resolution(self)` | 执行当前 API 的主要处理流程。 |
| `resolution(self, value)` | 执行当前 API 的主要处理流程。 |
| `description(self)` | 执行当前 API 的主要处理流程。 |
| `description(self, value)` | 执行当前 API 的主要处理流程。 |
| `part(self)` | 部位。兼容旧版 resolution。 |
| `part(self, value)` | 执行当前 API 的主要处理流程。 |
| `function(self)` | 功能。兼容旧版 description。 |
| `function(self, value)` | 执行当前 API 的主要处理流程。 |
| `index(self)` | 执行当前 API 的主要处理流程。 |
| `index(self, value)` | 执行当前 API 的主要处理流程。 |
| `name(self)` | 执行当前 API 的主要处理流程。 |
| `normalize_side(cls, side)` | 将方向统一成 lf / rt / md。 |
| `create_name(cls, node_type, side, part, function, index=1)` | 根据标准规则创建名称。 |
| `get_next_index(cls, node_type, side, part, function)` | 获取场景中同类名称的下一个可用序号。 |
| `create_unique_name(cls, node_type, side, part, function)` | 创建场景中下一个可用的标准名称。 |
| `parse_name(cls, name)` | 将标准名称拆分并返回字典。 |
| `mirror_name(cls, name)` | 返回名称的左右镜像名称，不修改 Maya 节点。 |
| `compose(self)` | 根据当前成员变量组合名称。 |
| `decompose(self)` | 拆分标准名称。 |
| `flip(self)` | 翻转名称方向：lf <-> rt，同时兼容旧版 l <-> r。 |
| `set_rename(self, new_name)` | 将当前选择的节点重命名。 |
| `add_prefix(self, prefix)` | 给当前节点添加前缀。 |
| `add_suffix(self, suffix)` | 给当前节点添加后缀。 |
| `add_hierarchy_prefix(self, prefix)` | 给当前选择层级添加前缀。 |
| `add_hierarchy_suffix(self, suffix)` | 给当前选择层级添加后缀。 |
| `search_replace_name(self, search, replace)` | 根据关键字搜索替换当前节点名称。 |
| `rename_to_name(self, new_name)` | 重命名为指定名称。 |
| `regex_search_replace_name(self, search, replace)` | 根据正则表达式搜索替换名称。 |
| `print_duplicate_object()` | 检查并列出场景中所有重名节点。 |
| `rename_duplicate_object()` | 检查并重新命名场景内所有重名节点。 |

## Classes 详细 API

### `Name`

Rig 节点命名类。

#### `__init__()`

**作用**

初始化当前对象，并准备运行时需要的状态和成员。

**Signature**

```python
__init__(self, name=None, type=None, side=None, resolution=None, description=None, index=None, part=None, function=None)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `name` | `str` | 否 | `None` | 已经存在的标准名称。如果给定，会自动拆分。 |
| `type` | `str` | 否 | `None` | 旧接口，节点类型。 |
| `side` | `str` | 否 | `None` | 方向。 |
| `resolution` | `str` | 否 | `None` | 旧接口，对应新的 part。 |
| `description` | `str` | 否 | `None` | 旧接口，对应新的 function。 |
| `index` | `int` | 否 | `None` | 序号。 |
| `part` | `str` | 否 | `None` | 新接口，部位。 |
| `function` | `str` | 否 | `None` | 新接口，功能。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()
```

#### `type()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
type(self)
```

**参数**

无。

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.type()
```

#### `type()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
type(self, value)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `value` | `float` | 是 | `—` | 需要读取、写入或参与计算的数值。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.type(
    value=1.0,
)
```

#### `side()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
side(self)
```

**参数**

无。

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.side()
```

#### `side()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
side(self, value)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `value` | `float` | 是 | `—` | 需要读取、写入或参与计算的数值。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.side(
    value=1.0,
)
```

#### `resolution()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
resolution(self)
```

**参数**

无。

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.resolution()
```

#### `resolution()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
resolution(self, value)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `value` | `float` | 是 | `—` | 需要读取、写入或参与计算的数值。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.resolution(
    value=1.0,
)
```

#### `description()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
description(self)
```

**参数**

无。

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.description()
```

#### `description()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
description(self, value)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `value` | `float` | 是 | `—` | 需要读取、写入或参与计算的数值。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.description(
    value=1.0,
)
```

#### `part()`

**作用**

部位。兼容旧版 resolution。

**Signature**

```python
part(self)
```

**参数**

无。

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.part()
```

#### `part()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
part(self, value)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `value` | `float` | 是 | `—` | 需要读取、写入或参与计算的数值。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.part(
    value=1.0,
)
```

#### `function()`

**作用**

功能。兼容旧版 description。

**Signature**

```python
function(self)
```

**参数**

无。

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.function()
```

#### `function()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
function(self, value)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `value` | `float` | 是 | `—` | 需要读取、写入或参与计算的数值。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.function(
    value=1.0,
)
```

#### `index()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
index(self)
```

**参数**

无。

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.index()
```

#### `index()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
index(self, value)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `value` | `float` | 是 | `—` | 需要读取、写入或参与计算的数值。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.index(
    value=1.0,
)
```

#### `name()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
name(self)
```

**参数**

无。

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.name()
```

#### `normalize_side()`

**作用**

将方向统一成 lf / rt / md。

示例：
    l       -> lf
    left    -> lf
    r       -> rt
    center  -> md
    m       -> md

**Signature**

```python
normalize_side(cls, side)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `side` | `str` | 是 | `—` | 方向标记，常用值为 lf、rt 或 md。 |

**返回值**

str | object:
        当前 API 查询或处理后得到的字符串结果。

**异常**

- `ValueError`：输入数据、场景状态或操作条件不满足要求时抛出。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.normalize_side(
    side="side",
)
```

#### `create_name()`

**作用**

根据标准规则创建名称。

标准：
    [类型]_[方向]_[部位]_[功能]_[序号]

**Signature**

```python
create_name(cls, node_type, side, part, function, index=1)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `node_type` | `str` | 是 | `—` | 需要创建、查询或过滤的 Maya Node Type。 |
| `side` | `str` | 是 | `—` | 方向标记，常用值为 lf、rt 或 md。 |
| `part` | `str` | 是 | `—` | Face / Rig 命名中的部位 Token，例如 lip、brow、eye、jaw。 |
| `function` | `str \| callable` | 是 | `—` | 当前 API 使用的功能 Token 或执行函数；在命名 API 中表示 function 段，在工具 API 中表示 Callable。 |
| `index` | `int` | 否 | `1` | 目标元素或节点的序号。 |

**返回值**

object:
        创建或构建完成后的 Maya / Rig 对象或 Build Result。

**异常**

- `ValueError`：输入数据、场景状态或操作条件不满足要求时抛出。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.create_name(
    node_type="node_type",
    side="side",
    part=...,
    function=...,
)
```

#### `get_next_index()`

**作用**

获取场景中同类名称的下一个可用序号。

**Signature**

```python
get_next_index(cls, node_type, side, part, function)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `node_type` | `str` | 是 | `—` | 需要创建、查询或过滤的 Maya Node Type。 |
| `side` | `str` | 是 | `—` | 方向标记，常用值为 lf、rt 或 md。 |
| `part` | `str` | 是 | `—` | Face / Rig 命名中的部位 Token，例如 lip、brow、eye、jaw。 |
| `function` | `str \| callable` | 是 | `—` | 当前 API 使用的功能 Token 或执行函数；在命名 API 中表示 function 段，在工具 API 中表示 Callable。 |

**返回值**

object:
        当前查询匹配到的 Maya / Rig 数据；没有结果时按 API 约定返回空值。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.get_next_index(
    node_type="node_type",
    side="side",
    part=...,
    function=...,
)
```

#### `create_unique_name()`

**作用**

创建场景中下一个可用的标准名称。

**Signature**

```python
create_unique_name(cls, node_type, side, part, function)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `node_type` | `str` | 是 | `—` | 需要创建、查询或过滤的 Maya Node Type。 |
| `side` | `str` | 是 | `—` | 方向标记，常用值为 lf、rt 或 md。 |
| `part` | `str` | 是 | `—` | Face / Rig 命名中的部位 Token，例如 lip、brow、eye、jaw。 |
| `function` | `str \| callable` | 是 | `—` | 当前 API 使用的功能 Token 或执行函数；在命名 API 中表示 function 段，在工具 API 中表示 Callable。 |

**返回值**

object:
        创建或构建完成后的 Maya / Rig 对象或 Build Result。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.create_unique_name(
    node_type="node_type",
    side="side",
    part=...,
    function=...,
)
```

#### `parse_name()`

**作用**

将标准名称拆分并返回字典。

**Signature**

```python
parse_name(cls, name)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `name` | `str` | 是 | `—` | 创建或查询时使用的节点名称。 |

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.parse_name(
    name="name",
)
```

#### `mirror_name()`

**作用**

返回名称的左右镜像名称，不修改 Maya 节点。

**Signature**

```python
mirror_name(cls, name)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `name` | `str` | 是 | `—` | 创建或查询时使用的节点名称。 |

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.mirror_name(
    name="name",
)
```

#### `compose()`

**作用**

根据当前成员变量组合名称。

**Signature**

```python
compose(self)
```

**参数**

无。

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.compose()
```

#### `decompose()`

**作用**

拆分标准名称。

支持功能字段包含下划线，例如：
    grp_md_face_rig_nodes_001

**Signature**

```python
decompose(self)
```

**参数**

无。

**返回值**

bool:
        当前操作成功或目标状态满足要求时返回 True，否则返回 False。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.decompose()
```

#### `flip()`

**作用**

翻转名称方向：lf <-> rt，同时兼容旧版 l <-> r。

**Signature**

```python
flip(self)
```

**参数**

无。

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.flip()
```

#### `set_rename()`

**作用**

将当前选择的节点重命名。

**Signature**

```python
set_rename(self, new_name)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `new_name` | `str` | 是 | `—` | `new_name` 对应的 Maya 节点或资源名称。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.set_rename(
    new_name="new_name",
)
```

#### `add_prefix()`

**作用**

给当前节点添加前缀。

**Signature**

```python
add_prefix(self, prefix)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `prefix` | `str` | 是 | `—` | 添加到 Maya 节点名称前部的 Prefix。 |

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.add_prefix(
    prefix=...,
)
```

#### `add_suffix()`

**作用**

给当前节点添加后缀。

**Signature**

```python
add_suffix(self, suffix)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `suffix` | `str` | 是 | `—` | 添加到 Maya 节点名称尾部的 Suffix。 |

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.add_suffix(
    suffix=...,
)
```

#### `add_hierarchy_prefix()`

**作用**

给当前选择层级添加前缀。

**Signature**

```python
add_hierarchy_prefix(self, prefix)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `prefix` | `str` | 是 | `—` | 添加到 Maya 节点名称前部的 Prefix。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.add_hierarchy_prefix(
    prefix=...,
)
```

#### `add_hierarchy_suffix()`

**作用**

给当前选择层级添加后缀。

**Signature**

```python
add_hierarchy_suffix(self, suffix)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `suffix` | `str` | 是 | `—` | 添加到 Maya 节点名称尾部的 Suffix。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.add_hierarchy_suffix(
    suffix=...,
)
```

#### `search_replace_name()`

**作用**

根据关键字搜索替换当前节点名称。

**Signature**

```python
search_replace_name(self, search, replace)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `search` | `str` | 是 | `—` | 节点名称中需要查找并替换的字符串。 |
| `replace` | `bool` | 是 | `—` | 替换 Search 内容的新字符串。 |

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.search_replace_name(
    search=...,
    replace=True,
)
```

#### `rename_to_name()`

**作用**

重命名为指定名称。

**Signature**

```python
rename_to_name(self, new_name)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `new_name` | `str` | 是 | `—` | `new_name` 对应的 Maya 节点或资源名称。 |

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.rename_to_name(
    new_name="new_name",
)
```

#### `regex_search_replace_name()`

**作用**

根据正则表达式搜索替换名称。

**Signature**

```python
regex_search_replace_name(self, search, replace)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `search` | `str` | 是 | `—` | 节点名称中需要查找并替换的字符串。 |
| `replace` | `bool` | 是 | `—` | 替换 Search 内容的新字符串。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.regex_search_replace_name(
    search=...,
    replace=True,
)
```

#### `print_duplicate_object()`

**作用**

检查并列出场景中所有重名节点。

**Signature**

```python
print_duplicate_object()
```

**参数**

无。

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.print_duplicate_object()
```

#### `rename_duplicate_object()`

**作用**

检查并重新命名场景内所有重名节点。

**Signature**

```python
rename_duplicate_object()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import nameUtils

instance = nameUtils.Name()

result = instance.rename_duplicate_object()
```

## 源码位置

`core/bake/nameUtils.py`
