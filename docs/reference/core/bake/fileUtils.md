<!-- AUTO-GENERATED BY scripts/generate_mkdocs_reference.py -->

# `muziToolset.core.bake.fileUtils`

> 本页由 `scripts/generate_mkdocs_reference.py` 根据当前源码 AST 自动生成。
> 源码 Docstring 是参数、返回值、异常和示例说明的第一事实来源。

## 概览

**用途**

当前模块尚未提供完整模块摘要。

**模块定位**

Core 底层公共能力；不负责 UI 和完整 Rig Workflow。
## 常用任务

- 在多个 Tool / System 中复用同一套 Maya 底层能力时。
- 希望业务逻辑与 UI 分离，并保持可测试、可组合时。

## Import

```python
from muziToolset.core.bake import fileUtils
```

## API 一览

### Functions

| API | 作用 |
| --- | --- |
| `text()` | 对于文件操作的例子 |

### Class `File`

文件操作的类

| Method | 作用 |
| --- | --- |
| `show_file_select_dialog(self)` | 打开文件资源浏览器，让用户选择文件 |
| `load_file(self, load_method)` | 读取文件 load_method(str): 三种不同的读取方式，open, import, reference |
| `open_file(self)` | 打开当前 file。 |
| `import_file(self)` | 导入当前 file。 |
| `reference_file(self)` | 引用文件 |
| `load_from_given_path(self)` | 加载当前 from given path。 |
| `export_animation_json(self)` | 导出所选控制器的动画到 JSON 文件 来源: 37佬 |
| `import_animation_json(self)` | 从 JSON 文件导入动画数据并应用到相应的控制器 |
| `get_current_scene_path()` | 获取当前文件的绝对路径 :return: |
| `create_reference(self, name_space=None)` | 在maya里的当前文件创建引用 :param self.file_path: 需要引用的文件路径 :param name_space: 引用文件的名称空间 :return: |
| `fbxExport(self)` | 在 Maya 中导出所选物体为 FBX 文件的脚本,接受一个目标路径self.file_path参数。该路径指定了导出的 FBX 文件的保存位置。 : self.file_path: 该路径指定了导出的 FBX 文件的保存位置。 :r... |
| `import_test_animation_json(self)` | 从 JSON 文件导入动画数据并应用到相应的控制器，进行测试动画的导入 |

## Functions 详细 API

### `text()`

**作用**

对于文件操作的例子

**Signature**

```python
text()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import fileUtils

result = fileUtils.text()
```

## Classes 详细 API

### `File`

文件操作的类

#### `__init__()`

**作用**

初始化当前对象，并准备运行时需要的状态和成员。

**Signature**

```python
__init__(self, file_path=None)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `file_path` | `str` | 否 | `None` | 需要读取或写入的文件路径。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import fileUtils

instance = fileUtils.File()
```

#### `show_file_select_dialog()`

**作用**

打开文件资源浏览器，让用户选择文件

**Signature**

```python
show_file_select_dialog(self)
```

**参数**

无。

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import fileUtils

instance = fileUtils.File()

result = instance.show_file_select_dialog()
```

#### `load_file()`

**作用**

读取文件 load_method(str): 三种不同的读取方式，open, import, reference

**Signature**

```python
load_file(self, load_method)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `load_method` | `object` | 是 | `—` | 当前方法执行 Maya / Rig 操作时使用的 `load_method` 数据。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import fileUtils

instance = fileUtils.File()

result = instance.load_file(
    load_method=...,
)
```

#### `open_file()`

**作用**

打开当前 file。

**Signature**

```python
open_file(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import fileUtils

instance = fileUtils.File()

result = instance.open_file()
```

#### `import_file()`

**作用**

导入当前 file。

**Signature**

```python
import_file(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import fileUtils

instance = fileUtils.File()

result = instance.import_file()
```

#### `reference_file()`

**作用**

引用文件

**Signature**

```python
reference_file(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import fileUtils

instance = fileUtils.File()

result = instance.reference_file()
```

#### `load_from_given_path()`

**作用**

加载当前 from given path。

**Signature**

```python
load_from_given_path(self)
```

**参数**

无。

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import fileUtils

instance = fileUtils.File()

result = instance.load_from_given_path()
```

#### `export_animation_json()`

**作用**

导出所选控制器的动画到 JSON 文件 来源: 37佬

**Signature**

```python
export_animation_json(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import fileUtils

instance = fileUtils.File()

result = instance.export_animation_json()
```

#### `import_animation_json()`

**作用**

从 JSON 文件导入动画数据并应用到相应的控制器

**Signature**

```python
import_animation_json(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import fileUtils

instance = fileUtils.File()

result = instance.import_animation_json()
```

#### `get_current_scene_path()`

**作用**

获取当前文件的绝对路径 :return:

**Signature**

```python
get_current_scene_path()
```

**参数**

无。

**返回值**

object:
        当前查询匹配到的 Maya / Rig 数据；没有结果时按 API 约定返回空值。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import fileUtils

instance = fileUtils.File()

result = instance.get_current_scene_path()
```

#### `create_reference()`

**作用**

在maya里的当前文件创建引用 :param self.file_path: 需要引用的文件路径 :param name_space: 引用文件的名称空间 :return:

**Signature**

```python
create_reference(self, name_space=None)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `name_space` | `object` | 否 | `None` | 当前方法执行 Maya / Rig 操作时使用的 `name_space` 数据。 |

**返回值**

tuple:
        按当前 API 约定组织的结果元组。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import fileUtils

instance = fileUtils.File()

result = instance.create_reference()
```

#### `fbxExport()`

**作用**

在 Maya 中导出所选物体为 FBX 文件的脚本,接受一个目标路径self.file_path参数。该路径指定了导出的 FBX 文件的保存位置。 : self.file_path: 该路径指定了导出的 FBX 文件的保存位置。 :r...

**Signature**

```python
fbxExport(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import fileUtils

instance = fileUtils.File()

result = instance.fbxExport()
```

#### `import_test_animation_json()`

**作用**

从 JSON 文件导入动画数据并应用到相应的控制器，进行测试动画的导入

**Signature**

```python
import_test_animation_json(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import fileUtils

instance = fileUtils.File()

result = instance.import_test_animation_json()
```

## 源码位置

`core/bake/fileUtils.py`
