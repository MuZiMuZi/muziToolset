<!-- AUTO-GENERATED BY scripts/generate_mkdocs_reference.py -->

# `muziToolset.core.bake.quick_render`

> 本页由 `scripts/generate_mkdocs_reference.py` 根据当前源码 AST 自动生成。
> 源码 Docstring 是参数、返回值、异常和示例说明的第一事实来源。

## 概览

**用途**

当前模块尚未提供完整模块摘要。

**模块定位**

Core 底层公共能力；不负责 UI 和完整 Rig Workflow。
## 常用任务

- 在多个 Tool / System 中复用同一套 Maya 底层能力时。
- 希望业务逻辑与 UI 分离，并保持可测试、可组合时。

## Import

```python
from muziToolset.core.bake import quick_render
```

## API 一览

### Functions

| API | 作用 |
| --- | --- |
| `get_type_file(data_path, file_type=['ma', 'mb'], data_file_list=None, with_path=False)` | 根据给定的文件类型。获取目录及子目录下所有文件名或者文件路径 :param data_path: 给定的文件目录 :param file_type: 文件类型,可以为列表或者字符串 :param data_file_list: 返回的... |

## Functions 详细 API

### `get_type_file()`

**作用**

根据给定的文件类型。获取目录及子目录下所有文件名或者文件路径 :param data_path: 给定的文件目录 :param file_type: 文件类型,可以为列表或者字符串 :param data_file_list: 返回的...

**Signature**

```python
get_type_file(data_path, file_type=['ma', 'mb'], data_file_list=None, with_path=False)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `data_path` | `str` | 是 | `—` | `data_path` 对应的文件或目录路径。 |
| `file_type` | `list` | 否 | `['ma', 'mb']` | 当前方法按顺序处理的 `file_type` 数据集合。 |
| `data_file_list` | `list` | 否 | `None` | 当前方法需要保持顺序批量处理的数据列表。 |
| `with_path` | `bool` | 否 | `False` | `with_path` 对应的文件或目录路径。 |

**返回值**

object:
    当前查询匹配到的 Maya / Rig 数据；没有结果时按 API 约定返回空值。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import quick_render

result = quick_render.get_type_file(
    data_path="data_path",
)
```

## 源码位置

`core/bake/quick_render.py`
