<!-- AUTO-GENERATED BY scripts/generate_mkdocs_reference.py -->

# `muziToolset.core.bake.weightsUtils`

> 本页由 `scripts/generate_mkdocs_reference.py` 根据当前源码 AST 自动生成。
> 源码 Docstring 是参数、返回值、异常和示例说明的第一事实来源。

## 概览

**用途**

这是一个用来编写权重工具的基本类

**模块定位**

Core 底层公共能力；不负责 UI 和完整 Rig Workflow。
## 常用任务

- 在多个 Tool / System 中复用同一套 Maya 底层能力时。
- 希望业务逻辑与 UI 分离，并保持可测试、可组合时。

## Import

```python
from muziToolset.core.bake import weightsUtils
```

## API 一览

### Class `Weights`

源码暂未提供类说明。

| Method | 作用 |
| --- | --- |
| `save_skinWeights(self)` | 将蒙皮几何体对象的权重保存到给定权重文件夹，权重将保存在对象短名称下，并附加存储其蒙皮影响的文件 :return: |
| `load_skinWeights(self)` | 从给定权重文件夹加载蒙皮几何体对象的权重将从与对象短名称匹配的文件名加载权重，并添加其他文件以获取其影响 :return: |
| `copy_weight()` | Returns:复制权重，先选择需要复制的蒙皮权重物体，再加选需要复制权重的物体 |
| `rename_bs_sc()` | 批量重命名对象的蒙皮和混合变形节点 |
| `get_skin_node(self)` | 获取物体的蒙皮节点信息，返回蒙皮节点 |
| `get_skin_node_jnt(self)` | 查询并返回当前 skin node jnt。 |
| `rename_skin_node(self)` | 执行当前 API 的主要处理流程。 |
| `set_deformer_skin(self, jnt_list)` | 使用线变形的方式来生成链式关节的蒙皮信息，适用于有规律的条状物体，布料状物体等，可以刷手臂的链式关节权重。 jnt_list(list):需要绘制权重的关节列表 思路： 1.根据需要绘制权重的关节列表创建一根吸附的曲线 2.将需要绘制... |

## Classes 详细 API

### `Weights`

源码暂未提供类说明。

#### `__init__()`

**作用**

geo(str) : 需要进行蒙皮操作的物体

**Signature**

```python
__init__(self, geo)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `geo` | `object` | 是 | `—` | 当前方法执行 Maya / Rig 操作时使用的 `geo` 数据。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import weightsUtils

instance = weightsUtils.Weights(
    geo=...,
)
```

#### `save_skinWeights()`

**作用**

将蒙皮几何体对象的权重保存到给定权重文件夹，权重将保存在对象短名称下，并附加存储其蒙皮影响的文件 :return:

**Signature**

```python
save_skinWeights(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import weightsUtils

instance = weightsUtils.Weights(
    geo=...,
)

result = instance.save_skinWeights()
```

#### `load_skinWeights()`

**作用**

从给定权重文件夹加载蒙皮几何体对象的权重将从与对象短名称匹配的文件名加载权重，并添加其他文件以获取其影响 :return:

**Signature**

```python
load_skinWeights(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import weightsUtils

instance = weightsUtils.Weights(
    geo=...,
)

result = instance.load_skinWeights()
```

#### `copy_weight()`

**作用**

Returns:复制权重，先选择需要复制的蒙皮权重物体，再加选需要复制权重的物体

**Signature**

```python
copy_weight()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import weightsUtils

instance = weightsUtils.Weights(
    geo=...,
)

result = instance.copy_weight()
```

#### `rename_bs_sc()`

**作用**

批量重命名对象的蒙皮和混合变形节点

**Signature**

```python
rename_bs_sc()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import weightsUtils

instance = weightsUtils.Weights(
    geo=...,
)

result = instance.rename_bs_sc()
```

#### `get_skin_node()`

**作用**

获取物体的蒙皮节点信息，返回蒙皮节点

**Signature**

```python
get_skin_node(self)
```

**参数**

无。

**返回值**

object | None:
    当前查询匹配到的 Maya / Rig 数据；没有结果时按 API 约定返回空值。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import weightsUtils

instance = weightsUtils.Weights(
    geo=...,
)

result = instance.get_skin_node()
```

#### `get_skin_node_jnt()`

**作用**

查询并返回当前 skin node jnt。

**Signature**

```python
get_skin_node_jnt(self)
```

**参数**

无。

**返回值**

object:
    当前查询匹配到的 Maya / Rig 数据；没有结果时按 API 约定返回空值。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import weightsUtils

instance = weightsUtils.Weights(
    geo=...,
)

result = instance.get_skin_node_jnt()
```

#### `rename_skin_node()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
rename_skin_node(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import weightsUtils

instance = weightsUtils.Weights(
    geo=...,
)

result = instance.rename_skin_node()
```

#### `set_deformer_skin()`

**作用**

使用线变形的方式来生成链式关节的蒙皮信息，适用于有规律的条状物体，布料状物体等，可以刷手臂的链式关节权重。 jnt_list(list):需要绘制权重的关节列表 思路： 1.根据需要绘制权重的关节列表创建一根吸附的曲线 2.将需要绘制...

**Signature**

```python
set_deformer_skin(self, jnt_list)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `jnt_list` | `list` | 是 | `—` | 当前方法需要保持顺序批量处理的数据列表。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import weightsUtils

instance = weightsUtils.Weights(
    geo=...,
)

result = instance.set_deformer_skin(
    jnt_list=[],
)
```

## 源码位置

`core/bake/weightsUtils.py`
