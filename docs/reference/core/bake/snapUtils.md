<!-- AUTO-GENERATED BY scripts/generate_mkdocs_reference.py -->

# `muziToolset.core.bake.snapUtils`

> 本页由 `scripts/generate_mkdocs_reference.py` 根据当前源码 AST 自动生成。
> 源码 Docstring 是参数、返回值、异常和示例说明的第一事实来源。

## 概览

**用途**

这是一个吸附的。 用来吸附对应的位置。

**模块定位**

Core 底层公共能力；不负责 UI 和完整 Rig Workflow。
## 常用任务

- 在多个 Tool / System 中复用同一套 Maya 底层能力时。
- 希望业务逻辑与 UI 分离，并保持可测试、可组合时。

## Import

```python
from muziToolset.core.bake import snapUtils
```

## API 一览

### Class `Snap`

源码暂未提供类说明。

| Method | 作用 |
| --- | --- |
| `find_centerPos_pivot(self)` | 返回对象列表的中心位置. |
| `find_centerRet_pivot(self)` | 返回对象列表的选择中心位置. |
| `snap_to_PosCenter(self)` | 将要吸附中心位置的对象捕捉到对象列表的中心位置。 |
| `snap_to_RetCenter(self)` | 将要吸附中心旋转的对象捕捉到对象列表的中心旋转. |
| `snap(self)` | 根据选择的吸附模式，进行对应的吸附功能 :return: |
| `push_snip()` | 执行当前 API 的主要处理流程。 |

## Classes 详细 API

### `Snap`

源码暂未提供类说明。

#### `__init__()`

**作用**

初始化当前对象，并准备运行时需要的状态和成员。

**Signature**

```python
__init__(self, obj, objs_list, combo)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `obj` | `object` | 是 | `—` | Objects requiring adsorption position |
| `objs_list` | `list` | 是 | `—` | List of objects used as positioning reference to adsorb positions |
| `combo` | `object` | 是 | `—` | 当前方法执行 Maya / Rig 操作时使用的 `combo` 数据。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import snapUtils

instance = snapUtils.Snap(
    obj=...,
    objs_list=[],
    combo=...,
)
```

#### `find_centerPos_pivot()`

**作用**

返回对象列表的中心位置.

**Signature**

```python
find_centerPos_pivot(self)
```

**参数**

无。

**返回值**

objs_list: 对象列表的中心位置, [center_pos_x, center_pos_y, center_pos_z].

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import snapUtils

instance = snapUtils.Snap(
    obj=...,
    objs_list=[],
    combo=...,
)

result = instance.find_centerPos_pivot()
```

#### `find_centerRet_pivot()`

**作用**

返回对象列表的选择中心位置.

**Signature**

```python
find_centerRet_pivot(self)
```

**参数**

无。

**返回值**

objs_list: 对象列表的中心旋转, [center_Ret_x, center_Ret_y, center_Ret_z].

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import snapUtils

instance = snapUtils.Snap(
    obj=...,
    objs_list=[],
    combo=...,
)

result = instance.find_centerRet_pivot()
```

#### `snap_to_PosCenter()`

**作用**

将要吸附中心位置的对象捕捉到对象列表的中心位置。

**Signature**

```python
snap_to_PosCenter(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import snapUtils

instance = snapUtils.Snap(
    obj=...,
    objs_list=[],
    combo=...,
)

result = instance.snap_to_PosCenter()
```

#### `snap_to_RetCenter()`

**作用**

将要吸附中心旋转的对象捕捉到对象列表的中心旋转.

**Signature**

```python
snap_to_RetCenter(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import snapUtils

instance = snapUtils.Snap(
    obj=...,
    objs_list=[],
    combo=...,
)

result = instance.snap_to_RetCenter()
```

#### `snap()`

**作用**

根据选择的吸附模式，进行对应的吸附功能 :return:

**Signature**

```python
snap(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import snapUtils

instance = snapUtils.Snap(
    obj=...,
    objs_list=[],
    combo=...,
)

result = instance.snap()
```

#### `push_snip()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
push_snip()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import snapUtils

instance = snapUtils.Snap(
    obj=...,
    objs_list=[],
    combo=...,
)

result = instance.push_snip()
```

## 源码位置

`core/bake/snapUtils.py`
