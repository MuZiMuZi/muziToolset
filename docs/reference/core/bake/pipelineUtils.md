<!-- AUTO-GENERATED BY scripts/generate_mkdocs_reference.py -->

# `muziToolset.core.bake.pipelineUtils`

> 本页由 `scripts/generate_mkdocs_reference.py` 根据当前源码 AST 自动生成。
> 源码 Docstring 是参数、返回值、异常和示例说明的第一事实来源。

## 概览

**用途**

这是一个用来编写流程工具的类

**模块定位**

Core 底层公共能力；不负责 UI 和完整 Rig Workflow。
## 常用任务

- 在多个 Tool / System 中复用同一套 Maya 底层能力时。
- 希望业务逻辑与 UI 分离，并保持可测试、可组合时。

## Import

```python
from muziToolset.core.bake import pipelineUtils
```

## API 一览

### Class `Pipeline`

源码暂未提供类说明。

| Method | 作用 |
| --- | --- |
| `clear_keys()` | 清除场景内所有的动画关键帧 :return: |
| `add_face_tag()` | 将“isFace”标记添加到所选物体的属性上. |
| `remove_non_face_objs()` | “移除没有带face标志的物体. |
| `distence_between(node_a, node_b)` | 获取两个对象之间的距离. node_a(str): 对象a. node_b(str): 对象b. |
| `reset_control()` | 重置绑定系统的控制器上所有的数值. |
| `list_operation(list_a, list_b, operation='\|')` | 将两个列表的并集/差分/交集/对称_差分部分作为列表返回. 用于执行集合操作的 Python 函数，根据传入的操作符号（\|, &, -, ^）返回两个列表的并集、交集、差分或对称差分。 |
| `batch_Constraints_modle()` | 选择物体，批量制作约束。新添加创建关节来蒙皮物体 |
| `batch_Constraints_joint()` | 选择关节，批量制作约束。不需要新添加创建关节来蒙皮物体 |
| `create_constraints()` | 快速创建约束. 用法：先选择需要约束者，在选择被约束者 driver_obj：约束者 driven_obj：被约束者 |
| `select_constraints()` | 快速选择物体的约束节点 |
| `delete_constraints()` | 快速删除选择物体的约束节点 |
| `make_undo(func)` | 这是一个装饰器，用于将一系列操作的撤销 包裹成一个撤销操作 |
| `create_node(node_type, node_name, match=False, match_node=None)` | 根据给定的节点类型，在给定的位置生成新的节点。 node_type；创建的新的节点类型 node_name:创建的新的节点名称 match:是否吸附对应的位置 match_node:吸附对应的位置 |
| `create_native_script_job(event_name, callback)` | 创建当前 native script job。 |
| `create_constraint(driver, driven, point_value=False, orient_value=False, parent_value=True, scale_value=False, mo_value=True)` | 这是一个用于在 Maya 中创建约束（constraint）的函数。该函数接受以下参数： |
| `move(obj, pos)` | 位移物体到指定的位置 |
| `get_percentages(sample_count)` | 曲线的总长度为1，给定需要平分的点数量，返回每个点的位置信息 例子：get_percentages(5) == [0.0, 0.25, 0.5, 0.75, 1.0] :param sample_count: int. 需要平分的点数... |
| `get_dag_path(node=None)` | 这是一个用于在 Maya 中获取指定节点的 DAG（Directed Acyclic Graph）路径的函数。函数接受一个参数 node，即 Maya 的节点对象（node name），并返回该节点的 DAG 路径（DAG path）... |
| `get_point_on_curve(curve, sample_count)` | 获取具有均匀距离的nurbs曲线上的点信息 https://help.autodesk.com/view/MAYAUL/2018/ENU/?guid=__cpp_ref_class_m_fn_nurbs_curve_html |
| `create_joints_on_curve(curve, sample_count)` | 在 Maya 中创建均匀分布在曲线上的关节点的函数。函数接受两个参数： |
| `create_curve_on_joints(jnt_list, curve, degree=3)` | 根据给定关节点的位置生成曲线的函数 jnt_list(list):关节的列表 curve（str）：创建出来的曲线的名称 degree(int)：新曲线的阶数。默认值为3。请注意，您需要（阶数+1）个曲线点来创建可见的曲线跨度。你必须... |
| `create_surface_on_curve(curve, surface_node, spans=4, offset=0.2)` | 根据给定的曲线放样生成出曲面 curve(str):给定的曲线名称 surface_node（str）：生成出来的曲面的名称 spans（int）:重建后曲线的点数 offset（float）：曲线偏移的距离值 |
| `create_joint_follicle_on_surface(surf_node, side, description, jnt_number)` | 在给定的曲面上创建毛囊节点和关节节点 surf_node（str）:给定的曲面，需要创建毛囊节点和关节节点的曲面 side（str）:边 description(str)：描述 jnt_number(int):需要创建的关节节点数量 |
| `create_curve_on_polyToCurve(curve_name, degree=3)` | 根据模型上所选择的边，模型的边到曲线生成新的曲线，判断场景里是否已经生成过对应的曲线，如果有的话则将其删除，没有的话则新创建 |
| `get_curve_number(curve)` | 获取曲线的点数量的函数 curve(str):想要获取点数量的曲线名称 获得曲线的点数量:spans + degree |
| `create_eyelid_joints_on_curve(curve, eye_joint, up_object)` | 基于cv创建眼睑关节，并使用目标约束附加到曲线 |
| `attach_joints_on_curve(jnt_list, drive_curve, aim_curve, up_object, aim_type='object')` | 使用pointOnCurveInfo节点在曲线上附加关节 |
| `create_zip_lip(lip_ctrls, jaw_ctrl, upper_jnts, lower_jnts, zip_height=0.5, falloff=3)` | 给嘴唇添加拉链嘴的绑定 |
| `create_doble_constraint(driver, ctrl, weight=0.5)` | 制作需要调整权重值的约束，驱动的物体和控制器的zero组去约束driven组，并且调整权重值 driver(str):驱动的物体 ctrl(str):被驱动的控制器 weight(int):约束的权重值 |
| `create_logging(logger_name, file_name, formatter='%(asctime)s -%(name)s - %(levelname)s - %(message)s')` | 创建logging日志，用来记录各个模块的日志报错信息，方便于项目排查 logger_name(str):用来自定义logger模块的名称 formatter(str):设置保存日志的信息,例如format = '%(asctime)... |
| `create_set(object, set_name, set_parent=None)` | 给指定的对象创建 set 选择集来方便选择。 |
| `create_dynamic_curve_driven()` | 选择需要创建动力学化的曲线。创建动力学化曲线驱动头发 曲线节点的命名为 模型节点 + '_crv' |
| `get_selected_type(type='transform')` | # 获取当前所选择的物体，并且判断是否是已经给定的规定类型 # 如果是已经给定的规定类型的话则正常 # 如果不是给定的规定类型则报错并且提示物体的类型 |
| `copy_surface_create_geo()` | 将需要绘制权重的模型所选择的面制作一个简模出来，作为用以线变形的模型 |
| `duplicate_model(source_model, new_name=None, parent=None)` | 给定复制的模型名称，复制出新的模型 source_model（str）:复制来源的模型 new_name(str):复制出来的模型名称 parent（str）:复制出来的模型的父层级 |

## Classes 详细 API

### `Pipeline`

源码暂未提供类说明。

#### `__init__()`

**作用**

初始化当前对象，并准备运行时需要的状态和成员。

**Signature**

```python
__init__(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()
```

#### `clear_keys()`

**作用**

清除场景内所有的动画关键帧 :return:

**Signature**

```python
clear_keys()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.clear_keys()
```

#### `add_face_tag()`

**作用**

将“isFace”标记添加到所选物体的属性上.

**Signature**

```python
add_face_tag()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.add_face_tag()
```

#### `remove_non_face_objs()`

**作用**

“移除没有带face标志的物体.

**Signature**

```python
remove_non_face_objs()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.remove_non_face_objs()
```

#### `distence_between()`

**作用**

获取两个对象之间的距离. node_a(str): 对象a. node_b(str): 对象b.

:return
dist(float):两个对象之间的距离.

**Signature**

```python
distence_between(node_a, node_b)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `node_a` | `object` | 是 | `—` | 当前方法执行 Maya / Rig 操作时使用的 `node_a` 数据。 |
| `node_b` | `object` | 是 | `—` | 当前方法执行 Maya / Rig 操作时使用的 `node_b` 数据。 |

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.distence_between(
    node_a="node_a",
    node_b="node_b",
)
```

#### `reset_control()`

**作用**

重置绑定系统的控制器上所有的数值.

**Signature**

```python
reset_control()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.reset_control()
```

#### `list_operation()`

**作用**

将两个列表的并集/差分/交集/对称_差分部分作为列表返回. 用于执行集合操作的 Python 函数，根据传入的操作符号（|, &, -, ^）返回两个列表的并集、交集、差分或对称差分。

**Signature**

```python
list_operation(list_a, list_b, operation='|')
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `list_a` | `list/None` | 是 | `—` | 第一个列表. |
| `list_b` | `list/None` | 是 | `—` | 第二个列表. |
| `operation` | `str` | 否 | `'\|'` | 运算符号为 '\|', '&', '-', '^'. 具体而言，该函数的实现步骤如下： 1.将传入的两个列表（list_a 和 list_b）转换为集合（set_a 和 set_b）。 2.根据传入的操作符号执行相应的集合操作。 3.将结果转换回列表并返回。 |

**返回值**

list: 作为列表的两个列表的并集/差分/交集/对称_差分部分.

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.list_operation(
    list_a=[],
    list_b=[],
)
```

#### `batch_Constraints_modle()`

**作用**

选择物体，批量制作约束。新添加创建关节来蒙皮物体

**Signature**

```python
batch_Constraints_modle()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.batch_Constraints_modle()
```

#### `batch_Constraints_joint()`

**作用**

选择关节，批量制作约束。不需要新添加创建关节来蒙皮物体

**Signature**

```python
batch_Constraints_joint()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.batch_Constraints_joint()
```

#### `create_constraints()`

**作用**

快速创建约束. 用法：先选择需要约束者，在选择被约束者 driver_obj：约束者 driven_obj：被约束者

**Signature**

```python
create_constraints()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.create_constraints()
```

#### `select_constraints()`

**作用**

快速选择物体的约束节点

**Signature**

```python
select_constraints()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.select_constraints()
```

#### `delete_constraints()`

**作用**

快速删除选择物体的约束节点

**Signature**

```python
delete_constraints()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.delete_constraints()
```

#### `make_undo()`

**作用**

这是一个装饰器，用于将一系列操作的撤销 包裹成一个撤销操作

**Signature**

```python
make_undo(func)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `func` | `object` | 是 | `—` | 当前方法执行 Maya / Rig 操作时使用的 `func` 数据。 |

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.make_undo(
    func=...,
)
```

#### `create_node()`

**作用**

根据给定的节点类型，在给定的位置生成新的节点。 node_type；创建的新的节点类型 node_name:创建的新的节点名称 match:是否吸附对应的位置 match_node:吸附对应的位置

**Signature**

```python
create_node(node_type, node_name, match=False, match_node=None)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `node_type` | `str` | 是 | `—` | 需要创建、查询或过滤的 Maya Node Type。 |
| `node_name` | `str` | 是 | `—` | `node_name` 对应的 Maya 节点或资源名称。 |
| `match` | `bool` | 否 | `False` | 控制当前方法中的 `match` 选项是否启用。 |
| `match_node` | `object` | 否 | `None` | 当前方法执行 Maya / Rig 操作时使用的 `match_node` 数据。 |

**返回值**

object:
        创建或构建完成后的 Maya / Rig 对象或 Build Result。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.create_node(
    node_type="node_type",
    node_name="node_name",
)
```

#### `create_native_script_job()`

**作用**

创建当前 native script job。

**Signature**

```python
create_native_script_job(event_name, callback)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `event_name` | `str` | 是 | `—` | `event_name` 对应的 Maya 节点或资源名称。 |
| `callback` | `object` | 是 | `—` | 当前方法执行 Maya / Rig 操作时使用的 `callback` 数据。 |

**返回值**

object:
        创建或构建完成后的 Maya / Rig 对象或 Build Result。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.create_native_script_job(
    event_name="event_name",
    callback=...,
)
```

#### `create_constraint()`

**作用**

这是一个用于在 Maya 中创建约束（constraint）的函数。该函数接受以下参数：

driver(str): 约束的驱动对象。
driven(str): 被约束的对象。
point_value(bool): 布尔值，指示是否创建点约束（默认为 False）。
orient_value(bool): 布尔值，指示是否创建方向约束（默认为 False）。
parent_value(bool): 布尔值，指示是否创建父级约束（默认为 True）。
scale_value(bool): 布尔值，指示是否创建缩放约束（默认为 False）。
mo_value(bool): 布尔值，指示是否使用移动轴心（默认为 True）。

**Signature**

```python
create_constraint(driver, driven, point_value=False, orient_value=False, parent_value=True, scale_value=False, mo_value=True)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `driver` | `str` | 是 | `—` | 作为驱动端的 Maya 节点名称。 |
| `driven` | `str` | 是 | `—` | 作为被驱动端的 Maya 节点名称。 |
| `point_value` | `bool` | 否 | `False` | 控制当前方法中的 `point_value` 选项是否启用。 |
| `orient_value` | `bool` | 否 | `False` | 控制当前方法中的 `orient_value` 选项是否启用。 |
| `parent_value` | `bool` | 否 | `True` | 控制当前方法中的 `parent_value` 选项是否启用。 |
| `scale_value` | `bool` | 否 | `False` | Controller Shape CV 使用的统一或 XYZ Scale 值。 |
| `mo_value` | `bool` | 否 | `True` | 控制当前方法中的 `mo_value` 选项是否启用。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.create_constraint(
    driver=...,
    driven=...,
)
```

#### `move()`

**作用**

位移物体到指定的位置

:param obj: str. maya的对象
:param pos: list. 位置信息 x, y and z

**Signature**

```python
move(obj, pos)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `obj` | `str` | 是 | `—` | 当前操作使用的 Maya DAG 节点或场景对象。 |
| `pos` | `object` | 是 | `—` | 当前方法执行 Maya / Rig 操作时使用的 `pos` 数据。 |

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.move(
    obj=...,
    pos=...,
)
```

#### `get_percentages()`

**作用**

曲线的总长度为1，给定需要平分的点数量，返回每个点的位置信息 例子：get_percentages(5) == [0.0, 0.25, 0.5, 0.75, 1.0] :param sample_count: int. 需要平分的点数...

**Signature**

```python
get_percentages(sample_count)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `sample_count` | `int` | 是 | `—` | 当前构建、采样或查询过程使用的元素数量。 |

**返回值**

object | list:
        按当前 API 约定顺序返回的结果列表。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.get_percentages(
    sample_count=1,
)
```

#### `get_dag_path()`

**作用**

这是一个用于在 Maya 中获取指定节点的 DAG（Directed Acyclic Graph）路径的函数。函数接受一个参数 node，即 Maya 的节点对象（node name），并返回该节点的 DAG 路径（DAG path）...

**Signature**

```python
get_dag_path(node=None)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `node` | `str` | 否 | `None` | 需要查询或处理的 Maya 节点名称。 |

**返回值**

object:
        当前查询匹配到的 Maya / Rig 数据；没有结果时按 API 约定返回空值。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.get_dag_path()
```

#### `get_point_on_curve()`

**作用**

获取具有均匀距离的nurbs曲线上的点信息 https://help.autodesk.com/view/MAYAUL/2018/ENU/?guid=__cpp_ref_class_m_fn_nurbs_curve_html

:param curve: str. nurbs曲线的名称
:param sample_count: int. 采样点的数量
:return: tuple. om.MPoint object and om.MVector object

**Signature**

```python
get_point_on_curve(curve, sample_count)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `curve` | `str` | 是 | `—` | 需要处理的 Maya Curve Transform 或 Shape 名称。 |
| `sample_count` | `int` | 是 | `—` | 当前构建、采样或查询过程使用的元素数量。 |

**返回值**

tuple:
        按当前 API 约定组织的结果元组。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.get_point_on_curve(
    curve="curve",
    sample_count=1,
)
```

#### `create_joints_on_curve()`

**作用**

在 Maya 中创建均匀分布在曲线上的关节点的函数。函数接受两个参数：

:param curve: str. 曲线的节点名称
:param sample_count: int. 采样点的数量
:return: list. 返回创建关节的列表

**Signature**

```python
create_joints_on_curve(curve, sample_count)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `curve` | `str` | 是 | `—` | 需要处理的 Maya Curve Transform 或 Shape 名称。 |
| `sample_count` | `int` | 是 | `—` | 当前构建、采样或查询过程使用的元素数量。 |

**返回值**

object:
        创建或构建完成后的 Maya / Rig 对象或 Build Result。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.create_joints_on_curve(
    curve="curve",
    sample_count=1,
)
```

#### `create_curve_on_joints()`

**作用**

根据给定关节点的位置生成曲线的函数 jnt_list(list):关节的列表 curve（str）：创建出来的曲线的名称 degree(int)：新曲线的阶数。默认值为3。请注意，您需要（阶数+1）个曲线点来创建可见的曲线跨度。你必须...

**Signature**

```python
create_curve_on_joints(jnt_list, curve, degree=3)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `jnt_list` | `list` | 是 | `—` | 当前方法需要保持顺序批量处理的数据列表。 |
| `curve` | `str` | 是 | `—` | 需要处理的 Maya Curve Transform 或 Shape 名称。 |
| `degree` | `int` | 否 | `3` | 创建或重建 NURBS Curve 使用的 Degree。 |

**返回值**

返回创建出来的曲线curve

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.create_curve_on_joints(
    jnt_list=[],
    curve="curve",
)
```

#### `create_surface_on_curve()`

**作用**

根据给定的曲线放样生成出曲面 curve(str):给定的曲线名称 surface_node（str）：生成出来的曲面的名称 spans（int）:重建后曲线的点数 offset（float）：曲线偏移的距离值

**Signature**

```python
create_surface_on_curve(curve, surface_node, spans=4, offset=0.2)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `curve` | `str` | 是 | `—` | 需要处理的 Maya Curve Transform 或 Shape 名称。 |
| `surface_node` | `object` | 是 | `—` | 当前方法执行 Maya / Rig 操作时使用的 `surface_node` 数据。 |
| `spans` | `int` | 否 | `4` | 当前 Maya / Rig 操作使用的 `spans` 整数参数。 |
| `offset` | `float` | 否 | `0.2` | 当前 Rig / Shape / Surface 操作使用的 Offset 数值或偏移向量。 |

**返回值**

返回曲面的节点

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.create_surface_on_curve(
    curve="curve",
    surface_node="surface_node",
)
```

#### `create_joint_follicle_on_surface()`

**作用**

在给定的曲面上创建毛囊节点和关节节点 surf_node（str）:给定的曲面，需要创建毛囊节点和关节节点的曲面 side（str）:边 description(str)：描述 jnt_number(int):需要创建的关节节点数量

return:follicle_dict:存储数据,fol_grp,jnt_grp,ctrl_grp,connect_list

**Signature**

```python
create_joint_follicle_on_surface(surf_node, side, description, jnt_number)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `surf_node` | `object` | 是 | `—` | 当前方法执行 Maya / Rig 操作时使用的 `surf_node` 数据。 |
| `side` | `str` | 是 | `—` | 方向标记，常用值为 lf、rt 或 md。 |
| `description` | `str` | 是 | `—` | UI Step / Section 中展示的功能说明文本。 |
| `jnt_number` | `int` | 是 | `—` | 当前构建、采样或查询过程使用的元素数量。 |

**返回值**

object:
        创建或构建完成后的 Maya / Rig 对象或 Build Result。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.create_joint_follicle_on_surface(
    surf_node="surf_node",
    side="side",
    description=...,
    jnt_number=1,
)
```

#### `create_curve_on_polyToCurve()`

**作用**

根据模型上所选择的边，模型的边到曲线生成新的曲线，判断场景里是否已经生成过对应的曲线，如果有的话则将其删除，没有的话则新创建

**Signature**

```python
create_curve_on_polyToCurve(curve_name, degree=3)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `curve_name` | `str` | 是 | `—` | `curve_name` 对应的 Maya 节点或资源名称。 |
| `degree` | `int` | 否 | `3` | 创建或重建 NURBS Curve 使用的 Degree。 |

**返回值**

生成的曲线

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.create_curve_on_polyToCurve(
    curve_name="curve_name",
)
```

#### `get_curve_number()`

**作用**

获取曲线的点数量的函数 curve(str):想要获取点数量的曲线名称 获得曲线的点数量:spans + degree

**Signature**

```python
get_curve_number(curve)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `curve` | `str` | 是 | `—` | 需要处理的 Maya Curve Transform 或 Shape 名称。 |

**返回值**

cv_num：返回曲线的点数量

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.get_curve_number(
    curve="curve",
)
```

#### `create_eyelid_joints_on_curve()`

**作用**

基于cv创建眼睑关节，并使用目标约束附加到曲线

**Signature**

```python
create_eyelid_joints_on_curve(curve, eye_joint, up_object)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `curve` | `str` | 是 | `—` | 眼睑曲线 |
| `eye_joint` | `str` | 是 | `—` | 眼睛的关节 |
| `up_object` | `str` | 是 | `—` | 向上的目标物体 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.create_eyelid_joints_on_curve(
    curve="curve",
    eye_joint="eye_joint",
    up_object="up_object",
)
```

#### `attach_joints_on_curve()`

**作用**

使用pointOnCurveInfo节点在曲线上附加关节

**Signature**

```python
attach_joints_on_curve(jnt_list, drive_curve, aim_curve, up_object, aim_type='object')
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `jnt_list` | `list` | 是 | `—` | 需要连接的关节列表 |
| `drive_curve` | `str` | 是 | `—` | 驱动关节的曲线 |
| `aim_curve` | `str` | 是 | `—` | 目标曲线 |
| `up_object` | `str` | 是 | `—` | 向上的参考向量的曲线 |
| `aim_type` | `str` | 否 | `'object'` | object/curve 物体或者是曲线 |

**返回值**

attach_dict: 将创建出来的attach对象返回出去

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.attach_joints_on_curve(
    jnt_list=[],
    drive_curve="drive_curve",
    aim_curve="aim_curve",
    up_object="up_object",
)
```

#### `create_zip_lip()`

**作用**

给嘴唇添加拉链嘴的绑定

**Signature**

```python
create_zip_lip(lip_ctrls, jaw_ctrl, upper_jnts, lower_jnts, zip_height=0.5, falloff=3)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `lip_ctrls` | `list` | 是 | `—` | 嘴唇控制器列表 |
| `jaw_ctrl` | `str` | 是 | `—` | 下巴控制器 |
| `upper_jnts` | `list` | 是 | `—` | 上嘴唇的关节列表 |
| `lower_jnts` | `list` | 是 | `—` | 下嘴唇的关节列表 |
| `zip_height` | `float` | 否 | `0.5` | zip的高度，默认为0.5，也就是上下嘴唇闭合到中间的高度 |
| `falloff` | `int` | 否 | `3` | 平滑值，嘴唇关节之间粘连的过渡值 |

**返回值**

zip_lip_dict(dict):返回出创建出来的内容

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.create_zip_lip(
    lip_ctrls=[],
    jaw_ctrl="jaw_ctrl",
    upper_jnts=[],
    lower_jnts=[],
)
```

#### `create_doble_constraint()`

**作用**

制作需要调整权重值的约束，驱动的物体和控制器的zero组去约束driven组，并且调整权重值 driver(str):驱动的物体 ctrl(str):被驱动的控制器 weight(int):约束的权重值

**Signature**

```python
create_doble_constraint(driver, ctrl, weight=0.5)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `driver` | `str` | 是 | `—` | 作为驱动端的 Maya 节点名称。 |
| `ctrl` | `object` | 是 | `—` | 当前方法执行 Maya / Rig 操作时使用的 `ctrl` 数据。 |
| `weight` | `float` | 否 | `0.5` | 当前计算、混合或变形使用的权重值。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.create_doble_constraint(
    driver=...,
    ctrl="ctrl",
)
```

#### `create_logging()`

**作用**

创建logging日志，用来记录各个模块的日志报错信息，方便于项目排查 logger_name(str):用来自定义logger模块的名称 formatter(str):设置保存日志的信息,例如format = '%(asctime)...

**Signature**

```python
create_logging(logger_name, file_name, formatter='%(asctime)s -%(name)s - %(levelname)s - %(message)s')
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `logger_name` | `str` | 是 | `—` | `logger_name` 对应的 Maya 节点或资源名称。 |
| `file_name` | `str` | 是 | `—` | `file_name` 对应的 Maya 节点或资源名称。 |
| `formatter` | `str` | 否 | `'%(asctime)s -%(name)s - %(levelname)s - %(message)s'` | 当前 Maya / Rig 操作使用的 `formatter` 名称或标记。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.create_logging(
    logger_name="logger_name",
    file_name="file_name",
)
```

#### `create_set()`

**作用**

给指定的对象创建 set 选择集来方便选择。

**Signature**

```python
create_set(object, set_name, set_parent=None)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `object` | `str/PyNode` | 是 | `—` | 需要添加到选择集的对象。 |
| `set_name` | `str` | 是 | `—` | 目标选择集名称。None / False / 空字符串时不执行。 |
| `set_parent` | `str/None` | 否 | `None` | 可选的父选择集名称。 |

**返回值**

str/None: 成功时返回 set 名称；参数无效时返回 None。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.create_set(
    object="object",
    set_name="set_name",
)
```

#### `create_dynamic_curve_driven()`

**作用**

选择需要创建动力学化的曲线。创建动力学化曲线驱动头发 曲线节点的命名为 模型节点 + '_crv'

**Signature**

```python
create_dynamic_curve_driven()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.create_dynamic_curve_driven()
```

#### `get_selected_type()`

**作用**

# 获取当前所选择的物体，并且判断是否是已经给定的规定类型 # 如果是已经给定的规定类型的话则正常 # 如果不是给定的规定类型则报错并且提示物体的类型

**Signature**

```python
get_selected_type(type='transform')
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `type` | `str` | 否 | `'transform'` | 当前 Maya / Rig 操作使用的 `type` 名称或标记。 |

**返回值**

object:
        当前查询匹配到的 Maya / Rig 数据；没有结果时按 API 约定返回空值。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.get_selected_type()
```

#### `copy_surface_create_geo()`

**作用**

将需要绘制权重的模型所选择的面制作一个简模出来，作为用以线变形的模型

high_geo(str):需要绘制权重的模型
skin_geo(str):用来复制权重的低模
返回：
    skin_geo(str):用来复制权重的低模

**Signature**

```python
copy_surface_create_geo()
```

**参数**

无。

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.copy_surface_create_geo()
```

#### `duplicate_model()`

**作用**

给定复制的模型名称，复制出新的模型 source_model（str）:复制来源的模型 new_name(str):复制出来的模型名称 parent（str）:复制出来的模型的父层级

**Signature**

```python
duplicate_model(source_model, new_name=None, parent=None)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `source_model` | `str` | 是 | `—` | 当前检查、绑定、复制或变形使用的模型 / Mesh 节点。 |
| `new_name` | `str` | 否 | `None` | `new_name` 对应的 Maya 节点或资源名称。 |
| `parent` | `str` | 否 | `None` | 父级 Maya 节点名称。 |

**返回值**

object | None:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import pipelineUtils

instance = pipelineUtils.Pipeline()

result = instance.duplicate_model(
    source_model="source_model",
)
```

## 源码位置

`core/bake/pipelineUtils.py`
