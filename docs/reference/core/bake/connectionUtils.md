<!-- AUTO-GENERATED BY scripts/generate_mkdocs_reference.py -->

# `muziToolset.core.bake.connectionUtils`

> 本页由 `scripts/generate_mkdocs_reference.py` 根据当前源码 AST 自动生成。
> 源码 Docstring 是参数、返回值、异常和示例说明的第一事实来源。

## 概览

**用途**

当前模块尚未提供完整模块摘要。

**模块定位**

Core 底层公共能力；不负责 UI 和完整 Rig Workflow。
## 常用任务

- 在多个 Tool / System 中复用同一套 Maya 底层能力时。
- 希望业务逻辑与 UI 分离，并保持可测试、可组合时。

## Import

```python
from muziToolset.core.bake import connectionUtils
```

## API 一览

### Class `Connection`

源码暂未提供类说明。

| Method | 作用 |
| --- | --- |
| `get_input_connection(self, attr, plus=True)` | 获取物体上的对应属性的输入连接 attr(str):获取输入需要查询连接的属性 return: input_connections 返回所有的输入连接 |
| `get_output_connection(self, object)` | 获取物体上的输出连接 self.object(str):获取输出连接的物体 return: self.input_connections 返回所有的输出连接 |
| `cheek_enough_obj_connection(self)` | 判断选择的对象是否数量足够可以进行连接 return： 返回驱动者和被驱动者:driver_obj,driven_obj_list |
| `cheek_obj_attrs_connection(self, driver_obj, source_attr, driven_obj, destination_attr)` | 检查：驱动者的属性是否能够成功连接上被驱动者的属性 检查项：1.是否存在对应的属性 2.属性之间的类型是否匹配 3.目标属性是否可以连接 driver_obj(str):作为驱动者的物体 source_attr(str):作为驱动者的... |
| `create_connections(self, driver_obj, source_attr, driven_obj, destination_attr)` | 驱动者的属性连接上被驱动者的属性 driver_obj(str):作为驱动者的物体 source_attr(str):作为驱动者的物体上驱动的属性 driven_obj(str):作为被驱动者的物体 destination_attr(... |
| `createt_connections_list(self, driver_obj, source_attr, driven_obj_list, destination_attr)` | 将驱动者的需要连接的属性连接给所有被驱动者需要连接的属性 driver_obj(str):作为驱动者的物体 source_attr(str):作为驱动者的物体上驱动的属性 driven_obj_list(str):作为被驱动者的物体列... |
| `create_connect_connections(self, source_attr, destination_attr)` | 选择多个物体，用于在第一个对象和列表中的所有其他对象之间建立连接 |
| `create_srt_connections(self, translate=True, rotation=True, scale=True, matrix=False)` | 用于在第一个对象和列表中的所有其他对象之间建立位移，旋转，缩放，矩阵等连接 objList(list):Maya节点名称列表，第一个节点将为驱动物体 translate(bool):是否连接所有位移的值 rotation(bool):... |
| `disconnect_attributes(node, attribute_list)` | #给定需要断开链接的属性列表进行断开链接 |
| `break_attr_connections(self)` | 断开所选择的通道盒上的属性的属性连接 |
| `break_connections(self, driver_obj, source_attr, driven_obj, destination_attr)` | 驱动者的属性连接上被驱动者的属性 driver_obj(str):作为驱动者的物体 source_attr(str):作为驱动者的物体上驱动的属性 driven_obj(str):作为被驱动者的物体 destination_attr(... |
| `break_connections_list(self, driver_obj, source_attr, driven_obj_list, destination_attr)` | 将驱动者的需要连接的属性连接给所有被驱动者需要连接的属性 driver_obj(str):作为驱动者的物体 source_attr(str):作为驱动者的物体上驱动的属性 driven_obj_list(str):作为被驱动者的物体列... |
| `break_connect_connections(self, source_attr, destination_attr)` | 选择多个物体，用于在第一个对象和列表中的所有其他对象之间断开连接 |
| `break_connect_srt_connections(self, translate=True, rotation=True, scale=True, matrix=False)` | 断开被驱动者上默认属性的断开连接 objList(list):Maya节点名称列表，第一个节点将为驱动物体 translate(bool):是否断开连接所有位移的值 rotation(bool):是否断开连接所有旋转的值 scale(... |

## Classes 详细 API

### `Connection`

源码暂未提供类说明。

#### `__init__()`

**作用**

初始化当前对象，并准备运行时需要的状态和成员。

**Signature**

```python
__init__(self, object)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `object` | `str` | 是 | `—` | 需要处理的 Maya 场景对象名称。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import connectionUtils

instance = connectionUtils.Connection(
    object="object",
)
```

#### `get_input_connection()`

**作用**

获取物体上的对应属性的输入连接 attr(str):获取输入需要查询连接的属性 return: input_connections 返回所有的输入连接

**Signature**

```python
get_input_connection(self, attr, plus=True)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `attr` | `str` | 是 | `—` | Maya Attribute 名称。 |
| `plus` | `bool` | 否 | `True` | 控制当前方法中的 `plus` 选项是否启用。 |

**返回值**

object:
        当前查询匹配到的 Maya / Rig 数据；没有结果时按 API 约定返回空值。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import connectionUtils

instance = connectionUtils.Connection(
    object="object",
)

result = instance.get_input_connection(
    attr="attr",
)
```

#### `get_output_connection()`

**作用**

获取物体上的输出连接 self.object(str):获取输出连接的物体 return: self.input_connections 返回所有的输出连接

**Signature**

```python
get_output_connection(self, object)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `object` | `str` | 是 | `—` | 需要处理的 Maya 场景对象名称。 |

**返回值**

object:
        当前查询匹配到的 Maya / Rig 数据；没有结果时按 API 约定返回空值。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import connectionUtils

instance = connectionUtils.Connection(
    object="object",
)

result = instance.get_output_connection(
    object="object",
)
```

#### `cheek_enough_obj_connection()`

**作用**

判断选择的对象是否数量足够可以进行连接 return： 返回驱动者和被驱动者:driver_obj,driven_obj_list

**Signature**

```python
cheek_enough_obj_connection(self)
```

**参数**

无。

**返回值**

tuple | bool:
        当前操作成功或目标状态满足要求时返回 True，否则返回 False。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import connectionUtils

instance = connectionUtils.Connection(
    object="object",
)

result = instance.cheek_enough_obj_connection()
```

#### `cheek_obj_attrs_connection()`

**作用**

检查：驱动者的属性是否能够成功连接上被驱动者的属性 检查项：1.是否存在对应的属性 2.属性之间的类型是否匹配 3.目标属性是否可以连接 driver_obj(str):作为驱动者的物体 source_attr(str):作为驱动者的...

**Signature**

```python
cheek_obj_attrs_connection(self, driver_obj, source_attr, driven_obj, destination_attr)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `driver_obj` | `object` | 是 | `—` | 当前方法执行 Maya / Rig 操作时使用的 `driver_obj` 数据。 |
| `source_attr` | `str` | 是 | `—` | 驱动端完整 Maya Plug，例如 `ctrl.translateX`。 |
| `driven_obj` | `object` | 是 | `—` | 当前方法执行 Maya / Rig 操作时使用的 `driven_obj` 数据。 |
| `destination_attr` | `str` | 是 | `—` | 接收连接的完整 Maya Plug，例如 `jnt.rotateY`。 |

**返回值**

object:
        当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import connectionUtils

instance = connectionUtils.Connection(
    object="object",
)

result = instance.cheek_obj_attrs_connection(
    driver_obj=...,
    source_attr="source_attr",
    driven_obj=...,
    destination_attr="destination_attr",
)
```

#### `create_connections()`

**作用**

驱动者的属性连接上被驱动者的属性 driver_obj(str):作为驱动者的物体 source_attr(str):作为驱动者的物体上驱动的属性 driven_obj(str):作为被驱动者的物体 destination_attr(...

**Signature**

```python
create_connections(self, driver_obj, source_attr, driven_obj, destination_attr)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `driver_obj` | `object` | 是 | `—` | 当前方法执行 Maya / Rig 操作时使用的 `driver_obj` 数据。 |
| `source_attr` | `str` | 是 | `—` | 驱动端完整 Maya Plug，例如 `ctrl.translateX`。 |
| `driven_obj` | `object` | 是 | `—` | 当前方法执行 Maya / Rig 操作时使用的 `driven_obj` 数据。 |
| `destination_attr` | `str` | 是 | `—` | 接收连接的完整 Maya Plug，例如 `jnt.rotateY`。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import connectionUtils

instance = connectionUtils.Connection(
    object="object",
)

result = instance.create_connections(
    driver_obj=...,
    source_attr="source_attr",
    driven_obj=...,
    destination_attr="destination_attr",
)
```

#### `createt_connections_list()`

**作用**

将驱动者的需要连接的属性连接给所有被驱动者需要连接的属性 driver_obj(str):作为驱动者的物体 source_attr(str):作为驱动者的物体上驱动的属性 driven_obj_list(str):作为被驱动者的物体列...

**Signature**

```python
createt_connections_list(self, driver_obj, source_attr, driven_obj_list, destination_attr)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `driver_obj` | `object` | 是 | `—` | 当前方法执行 Maya / Rig 操作时使用的 `driver_obj` 数据。 |
| `source_attr` | `str` | 是 | `—` | 驱动端完整 Maya Plug，例如 `ctrl.translateX`。 |
| `driven_obj_list` | `list` | 是 | `—` | 当前方法需要保持顺序批量处理的数据列表。 |
| `destination_attr` | `str` | 是 | `—` | 接收连接的完整 Maya Plug，例如 `jnt.rotateY`。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import connectionUtils

instance = connectionUtils.Connection(
    object="object",
)

result = instance.createt_connections_list(
    driver_obj=...,
    source_attr="source_attr",
    driven_obj_list=[],
    destination_attr="destination_attr",
)
```

#### `create_connect_connections()`

**作用**

选择多个物体，用于在第一个对象和列表中的所有其他对象之间建立连接

**Signature**

```python
create_connect_connections(self, source_attr, destination_attr)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `source_attr` | `str` | 是 | `—` | 驱动端完整 Maya Plug，例如 `ctrl.translateX`。 |
| `destination_attr` | `str` | 是 | `—` | 接收连接的完整 Maya Plug，例如 `jnt.rotateY`。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import connectionUtils

instance = connectionUtils.Connection(
    object="object",
)

result = instance.create_connect_connections(
    source_attr="source_attr",
    destination_attr="destination_attr",
)
```

#### `create_srt_connections()`

**作用**

用于在第一个对象和列表中的所有其他对象之间建立位移，旋转，缩放，矩阵等连接 objList(list):Maya节点名称列表，第一个节点将为驱动物体 translate(bool):是否连接所有位移的值 rotation(bool):...

**Signature**

```python
create_srt_connections(self, translate=True, rotation=True, scale=True, matrix=False)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `translate` | `bool` | 否 | `True` | 是否处理 Translate 通道。 |
| `rotation` | `bool` | 否 | `True` | Jnt / Transform 使用的 XYZ Rotation。 |
| `scale` | `bool` | 否 | `True` | 是否处理 Scale 通道。 |
| `matrix` | `bool` | 否 | `False` | 用于 Transform、Constraint 或空间计算的 4x4 Matrix 数据。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import connectionUtils

instance = connectionUtils.Connection(
    object="object",
)

result = instance.create_srt_connections()
```

#### `disconnect_attributes()`

**作用**

#给定需要断开链接的属性列表进行断开链接

**Signature**

```python
disconnect_attributes(node, attribute_list)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `node` | `str` | 是 | `—` | 需要查询或处理的 Maya 节点名称。 |
| `attribute_list` | `list` | 是 | `—` | 当前方法需要保持顺序批量处理的数据列表。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import connectionUtils

instance = connectionUtils.Connection(
    object="object",
)

result = instance.disconnect_attributes(
    node="node",
    attribute_list=[],
)
```

#### `break_attr_connections()`

**作用**

断开所选择的通道盒上的属性的属性连接

**Signature**

```python
break_attr_connections(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import connectionUtils

instance = connectionUtils.Connection(
    object="object",
)

result = instance.break_attr_connections()
```

#### `break_connections()`

**作用**

驱动者的属性连接上被驱动者的属性 driver_obj(str):作为驱动者的物体 source_attr(str):作为驱动者的物体上驱动的属性 driven_obj(str):作为被驱动者的物体 destination_attr(...

**Signature**

```python
break_connections(self, driver_obj, source_attr, driven_obj, destination_attr)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `driver_obj` | `object` | 是 | `—` | 当前方法执行 Maya / Rig 操作时使用的 `driver_obj` 数据。 |
| `source_attr` | `str` | 是 | `—` | 驱动端完整 Maya Plug，例如 `ctrl.translateX`。 |
| `driven_obj` | `object` | 是 | `—` | 当前方法执行 Maya / Rig 操作时使用的 `driven_obj` 数据。 |
| `destination_attr` | `str` | 是 | `—` | 接收连接的完整 Maya Plug，例如 `jnt.rotateY`。 |

**返回值**

bool:
        当前操作成功或目标状态满足要求时返回 True，否则返回 False。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import connectionUtils

instance = connectionUtils.Connection(
    object="object",
)

result = instance.break_connections(
    driver_obj=...,
    source_attr="source_attr",
    driven_obj=...,
    destination_attr="destination_attr",
)
```

#### `break_connections_list()`

**作用**

将驱动者的需要连接的属性连接给所有被驱动者需要连接的属性 driver_obj(str):作为驱动者的物体 source_attr(str):作为驱动者的物体上驱动的属性 driven_obj_list(str):作为被驱动者的物体列...

**Signature**

```python
break_connections_list(self, driver_obj, source_attr, driven_obj_list, destination_attr)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `driver_obj` | `object` | 是 | `—` | 当前方法执行 Maya / Rig 操作时使用的 `driver_obj` 数据。 |
| `source_attr` | `str` | 是 | `—` | 驱动端完整 Maya Plug，例如 `ctrl.translateX`。 |
| `driven_obj_list` | `list` | 是 | `—` | 当前方法需要保持顺序批量处理的数据列表。 |
| `destination_attr` | `str` | 是 | `—` | 接收连接的完整 Maya Plug，例如 `jnt.rotateY`。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import connectionUtils

instance = connectionUtils.Connection(
    object="object",
)

result = instance.break_connections_list(
    driver_obj=...,
    source_attr="source_attr",
    driven_obj_list=[],
    destination_attr="destination_attr",
)
```

#### `break_connect_connections()`

**作用**

选择多个物体，用于在第一个对象和列表中的所有其他对象之间断开连接

**Signature**

```python
break_connect_connections(self, source_attr, destination_attr)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `source_attr` | `str` | 是 | `—` | 驱动端完整 Maya Plug，例如 `ctrl.translateX`。 |
| `destination_attr` | `str` | 是 | `—` | 接收连接的完整 Maya Plug，例如 `jnt.rotateY`。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import connectionUtils

instance = connectionUtils.Connection(
    object="object",
)

result = instance.break_connect_connections(
    source_attr="source_attr",
    destination_attr="destination_attr",
)
```

#### `break_connect_srt_connections()`

**作用**

断开被驱动者上默认属性的断开连接 objList(list):Maya节点名称列表，第一个节点将为驱动物体 translate(bool):是否断开连接所有位移的值 rotation(bool):是否断开连接所有旋转的值 scale(...

**Signature**

```python
break_connect_srt_connections(self, translate=True, rotation=True, scale=True, matrix=False)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `translate` | `bool` | 否 | `True` | 是否处理 Translate 通道。 |
| `rotation` | `bool` | 否 | `True` | Jnt / Transform 使用的 XYZ Rotation。 |
| `scale` | `bool` | 否 | `True` | 是否处理 Scale 通道。 |
| `matrix` | `bool` | 否 | `False` | 用于 Transform、Constraint 或空间计算的 4x4 Matrix 数据。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import connectionUtils

instance = connectionUtils.Connection(
    object="object",
)

result = instance.break_connect_srt_connections()
```

## 源码位置

`core/bake/connectionUtils.py`
