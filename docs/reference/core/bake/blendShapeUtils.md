<!-- AUTO-GENERATED BY scripts/generate_mkdocs_reference.py -->

# `muziToolset.core.bake.blendShapeUtils`

> 本页由 `scripts/generate_mkdocs_reference.py` 根据当前源码 AST 自动生成。
> 源码 Docstring 是参数、返回值、异常和示例说明的第一事实来源。

## 概览

**用途**

这是一个blendshape的模块。 用来对混合变形进行一系列修改的操作。

**模块定位**

Core 底层公共能力；不负责 UI 和完整 Rig Workflow。
## 常用任务

- 在多个 Tool / System 中复用同一套 Maya 底层能力时。
- 希望业务逻辑与 UI 分离，并保持可测试、可组合时。
- 创建、编辑或查询 BlendShape / 修型目标时。

## Import

```python
from muziToolset.core.bake import blendShapeUtils
```

## API 一览

### Class `BlendShape`

源码暂未提供类说明。

| Method | 作用 |
| --- | --- |
| `get_blendshape_node(self)` | 查询并返回当前 blendshape node。 |
| `get_blendshape_name(self)` | 查询并返回当前 blendshape name。 |
| `get_blendshape_weight(self)` | 查询并返回当前 blendshape weight。 |
| `get_blendshape_weight_list(self)` | 查询并返回当前 blendshape weight list。 |

## Classes 详细 API

### `BlendShape`

源码暂未提供类说明。

#### `__init__()`

**作用**

初始化当前对象，并准备运行时需要的状态和成员。

**Signature**

```python
__init__(self, model_node)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `model_node` | `object` | 是 | `—` | 当前方法执行 Maya / Rig 操作时使用的 `model_node` 数据。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import blendShapeUtils

instance = blendShapeUtils.BlendShape(
    model_node="model_node",
)
```

#### `get_blendshape_node()`

**作用**

查询并返回当前 blendshape node。

**Signature**

```python
get_blendshape_node(self)
```

**参数**

无。

**返回值**

object:
        当前查询匹配到的 Maya / Rig 数据；没有结果时按 API 约定返回空值。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import blendShapeUtils

instance = blendShapeUtils.BlendShape(
    model_node="model_node",
)

result = instance.get_blendshape_node()
```

#### `get_blendshape_name()`

**作用**

查询并返回当前 blendshape name。

**Signature**

```python
get_blendshape_name(self)
```

**参数**

无。

**返回值**

object:
        当前查询匹配到的 Maya / Rig 数据；没有结果时按 API 约定返回空值。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import blendShapeUtils

instance = blendShapeUtils.BlendShape(
    model_node="model_node",
)

result = instance.get_blendshape_name()
```

#### `get_blendshape_weight()`

**作用**

查询并返回当前 blendshape weight。

**Signature**

```python
get_blendshape_weight(self)
```

**参数**

无。

**返回值**

object:
        当前查询匹配到的 Maya / Rig 数据；没有结果时按 API 约定返回空值。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import blendShapeUtils

instance = blendShapeUtils.BlendShape(
    model_node="model_node",
)

result = instance.get_blendshape_weight()
```

#### `get_blendshape_weight_list()`

**作用**

查询并返回当前 blendshape weight list。

**Signature**

```python
get_blendshape_weight_list(self)
```

**参数**

无。

**返回值**

object:
        当前查询匹配到的 Maya / Rig 数据；没有结果时按 API 约定返回空值。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.core.bake import blendShapeUtils

instance = blendShapeUtils.BlendShape(
    model_node="model_node",
)

result = instance.get_blendshape_weight_list()
```

## 源码位置

`core/bake/blendShapeUtils.py`
