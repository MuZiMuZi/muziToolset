<!-- AUTO-GENERATED BY scripts/generate_mkdocs_reference.py -->

# `muziToolset.app.toolbox_catalog`

> 本页由 `scripts/generate_mkdocs_reference.py` 根据当前源码 AST 自动生成。
> 源码 Docstring 是参数、返回值、异常和示例说明的第一事实来源。

## 概览

**用途**

职责： 1. 保存工具显示名称、简介和分类文案； 2.

**模块定位**

应用入口层；负责主工具箱、窗口生命周期和工具调度。
## 常用任务

- 启动 MuziTools 主程序、工具箱或管理顶层窗口生命周期时。

## Import

```python
from muziToolset.app import toolbox_catalog
```

## API 一览

### Functions

| API | 作用 |
| --- | --- |
| `get_tool_display_name(tool_name)` | 返回工具卡片显示名称。 |
| `get_tool_description(tool_name)` | 返回工具卡片的一行功能说明。 |
| `get_tool_mode(tool_function)` | 读取 Tool Registry Runner 声明的运行模式。 |
| `get_tool_mode_display_name(tool_mode)` | 把工具运行模式转换成中文显示名称。 |

## 公共常量

| 名称 | 值 |
| --- | --- |
| `TOOL_MODE_UI` | `'ui'` |
| `TOOL_MODE_ACTION` | `'action'` |

## Functions 详细 API

### `get_tool_display_name()`

**作用**

返回工具卡片显示名称。

**Signature**

```python
get_tool_display_name(tool_name)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `tool_name` | `str` | 是 | `—` | Tool Registry 中的模块短名称。 |

**返回值**

str:
    中文显示名称；未登记时返回由模块名转换出的标题。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.app import toolbox_catalog

result = toolbox_catalog.get_tool_display_name(
    tool_name="tool_name",
)
```

### `get_tool_description()`

**作用**

返回工具卡片的一行功能说明。

**Signature**

```python
get_tool_description(tool_name)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `tool_name` | `str` | 是 | `—` | Tool Registry 中的模块短名称。 |

**返回值**

str:
    工具用途简介；未登记时返回通用运行说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.app import toolbox_catalog

result = toolbox_catalog.get_tool_description(
    tool_name="tool_name",
)
```

### `get_tool_mode()`

**作用**

读取 Tool Registry Runner 声明的运行模式。

**Signature**

```python
get_tool_mode(tool_function)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `tool_function` | `callable` | 是 | `—` | Tool Registry 创建的懒加载 Runner。 |

**返回值**

str:
    ``ui`` 或 ``action``；未声明时保持旧工具的 ``ui`` 行为。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.app import toolbox_catalog

result = toolbox_catalog.get_tool_mode(
    tool_function=...,
)
```

### `get_tool_mode_display_name()`

**作用**

把工具运行模式转换成中文显示名称。

**Signature**

```python
get_tool_mode_display_name(tool_mode)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `tool_mode` | `str` | 是 | `—` | ``ui`` 或 ``action`` 运行模式。 |

**返回值**

str:
    ``界面工具`` 或 ``直接执行``。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.app import toolbox_catalog

result = toolbox_catalog.get_tool_mode_display_name(
    tool_mode=...,
)
```

## 源码位置

`app/toolbox_catalog.py`
