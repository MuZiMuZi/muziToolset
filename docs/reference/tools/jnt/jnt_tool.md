<!-- AUTO-GENERATED BY scripts/generate_mkdocs_reference.py -->

# `muziToolset.tools.jnt.jnt_tool`

> 本页由 `scripts/generate_mkdocs_reference.py` 根据当前源码 AST 自动生成。
> 源码 Docstring 是参数、返回值、异常和示例说明的第一事实来源。

## 概览

**用途**

Maya 2023+ Jnt 工具面板。

**模块定位**

Tool 用户操作层；负责 UI、Selection 和调用底层能力。
## 常用任务

- 绑定师需要直接打开一个独立 Maya 小工具完成具体操作时。
- UI 负责收集 Selection / 参数，再调用 Core 或 Systems 时。

## Import

```python
from muziToolset.tools.jnt import jnt_tool
```

## API 一览

### Functions

| API | 作用 |
| --- | --- |
| `main()` | 创建或恢复 Jnt Tool，立即显示并返回 QWidget。 |

### Class `JntTool`

木子绑定工具集 Jnt 工具。

| Method | 作用 |
| --- | --- |
| `create_widgets(self)` | 创建窗口控件。 |
| `create_layouts(self)` | 创建滚动 Card 布局。 |
| `create_connections(self)` | 连接界面信号。 |
| `open_orient_options()` | 打开 Maya Jnt Orient Options。 |
| `open_mirror_options()` | 打开 Maya Mirror Jnt Options。 |
| `open_ik_handle_options()` | 打开 Maya IK Handle Options。 |
| `open_ik_spline_options()` | 打开 Maya IK Spline Options。 |
| `open_bind_skin_options()` | 打开 Maya Smooth Bind Skin Options。 |
| `open_detach_skin_options()` | 打开 Maya Detach Skin Options。 |
| `open_paint_skin_options()` | 打开 Maya Paint Skin Weights。 |
| `open_mirror_skin_options()` | 打开 Maya Mirror Skin Weights Options。 |
| `get_selected_jnts()` | 返回当前选择 Jnt；空选择时显示 Tool Warning。 |
| `set_jnt_size(self, value)` | 设置全场景 Jnt Radius。 |
| `show_axis_selected(self)` | 执行当前 API 的主要处理流程。 |
| `hide_axis_selected(self)` | 执行当前 API 的主要处理流程。 |
| `show_axis_hierarchy(self)` | 执行当前 API 的主要处理流程。 |
| `hide_axis_hierarchy(self)` | 执行当前 API 的主要处理流程。 |
| `show_axis_all(self)` | 执行当前 API 的主要处理流程。 |
| `hide_axis_all(self)` | 执行当前 API 的主要处理流程。 |
| `set_axis_visibility(visible, hierarchy, all_jnts)` | 设置选择、选择层级或场景全部 Jnt 的 Local Rotation Axis。 |
| `create_snap_jnts(self)` | 在当前选择的 Object / Component 世界位置创建 Jnt。 |
| `create_child_jnts(self)` | 为当前选择的 Transform / Jnt 创建同姿态 Child Jnt。 |
| `open_resample_tool()` | 通过统一 Window Manager 打开 Jnt Resample。 |
| `parent_selected_chain()` | 按照当前 Jnt Selection 顺序组成父子链。 |
| `create_jnts_on_curves(self)` | 根据选择 Curve 的 CV 创建 Jnt Chain。 |
| `create_jnts_on_edges(self)` | 把当前 Polygon Edge Selection 转 Curve 后创建 Jnt Chain。 |
| `enable_scale_compensate(self)` | 执行当前 API 的主要处理流程。 |
| `disable_scale_compensate(self)` | 执行当前 API 的主要处理流程。 |
| `set_scale_compensate(self, enabled)` | 设置选择 Jnt 的 Segment Scale Compensate。 |
| `show_orient(self)` | 执行当前 API 的主要处理流程。 |
| `hide_orient(self)` | 执行当前 API 的主要处理流程。 |
| `set_orient_visibility(self, visible)` | 显示或隐藏选择 Jnt 的 jointOrient Channel。 |
| `clear_jnt_orient(self)` | 清空选择 Jnt 的 jntOrientXYZ。 |
| `create_curve_on_jnts(self)` | 按照选择 Jnt 世界位置创建 Curve。 |
| `batch_parent_constraint()` | 按照 driver/driven 成对顺序批量创建 Parent Constraint。 |
| `copy_skin_weights()` | 把第一个选择模型 Skin Weight 复制到后续选择模型。 |

## Functions 详细 API

### `main()`

**作用**

创建或恢复 Jnt Tool，立即显示并返回 QWidget。

**Signature**

```python
main()
```

**参数**

无。

**返回值**

object:
    当前工具入口创建并显示的窗口或执行结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

result = jnt_tool.main()
```

## Classes 详细 API

### `JntTool`

木子绑定工具集 Jnt 工具。

#### `__init__()`

**作用**

创建 Jnt 工具窗口。

**Signature**

```python
__init__(self, parent=None)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `parent` | `str` | 否 | `None` | 父级 Maya 节点名称。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()
```

#### `create_widgets()`

**作用**

创建窗口控件。

**Signature**

```python
create_widgets(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.create_widgets()
```

#### `create_layouts()`

**作用**

创建滚动 Card 布局。

**Signature**

```python
create_layouts(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.create_layouts()
```

#### `create_connections()`

**作用**

连接界面信号。

**Signature**

```python
create_connections(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.create_connections()
```

#### `open_orient_options()`

**作用**

打开 Maya Jnt Orient Options。

**Signature**

```python
open_orient_options()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.open_orient_options()
```

#### `open_mirror_options()`

**作用**

打开 Maya Mirror Jnt Options。

**Signature**

```python
open_mirror_options()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.open_mirror_options()
```

#### `open_ik_handle_options()`

**作用**

打开 Maya IK Handle Options。

**Signature**

```python
open_ik_handle_options()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.open_ik_handle_options()
```

#### `open_ik_spline_options()`

**作用**

打开 Maya IK Spline Options。

**Signature**

```python
open_ik_spline_options()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.open_ik_spline_options()
```

#### `open_bind_skin_options()`

**作用**

打开 Maya Smooth Bind Skin Options。

**Signature**

```python
open_bind_skin_options()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.open_bind_skin_options()
```

#### `open_detach_skin_options()`

**作用**

打开 Maya Detach Skin Options。

**Signature**

```python
open_detach_skin_options()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.open_detach_skin_options()
```

#### `open_paint_skin_options()`

**作用**

打开 Maya Paint Skin Weights。

**Signature**

```python
open_paint_skin_options()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.open_paint_skin_options()
```

#### `open_mirror_skin_options()`

**作用**

打开 Maya Mirror Skin Weights Options。

**Signature**

```python
open_mirror_skin_options()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.open_mirror_skin_options()
```

#### `get_selected_jnts()`

**作用**

返回当前选择 Jnt；空选择时显示 Tool Warning。

**Signature**

```python
get_selected_jnts()
```

**参数**

无。

**返回值**

object:
    当前查询匹配到的 Maya / Rig 数据；没有结果时按 API 约定返回空值。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.get_selected_jnts()
```

#### `set_jnt_size()`

**作用**

设置全场景 Jnt Radius。

**Signature**

```python
set_jnt_size(self, value)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `value` | `float` | 是 | `—` | 需要读取、写入或参与计算的数值。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.set_jnt_size(
    value=1.0,
)
```

#### `show_axis_selected()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
show_axis_selected(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.show_axis_selected()
```

#### `hide_axis_selected()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
hide_axis_selected(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.hide_axis_selected()
```

#### `show_axis_hierarchy()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
show_axis_hierarchy(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.show_axis_hierarchy()
```

#### `hide_axis_hierarchy()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
hide_axis_hierarchy(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.hide_axis_hierarchy()
```

#### `show_axis_all()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
show_axis_all(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.show_axis_all()
```

#### `hide_axis_all()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
hide_axis_all(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.hide_axis_all()
```

#### `set_axis_visibility()`

**作用**

设置选择、选择层级或场景全部 Jnt 的 Local Rotation Axis。

**Signature**

```python
set_axis_visibility(visible, hierarchy, all_jnts)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `visible` | `bool` | 是 | `—` | Jnt / Guide / UI 元素是否保持可见。 |
| `hierarchy` | `bool \| str` | 是 | `—` | Jnt Tool 当前是否按 Skeleton Hierarchy 工作，或用于指定层级范围。 |
| `all_jnts` | `str \| list[str]` | 是 | `—` | 当前 Jnt Tool 已解析出的完整 Jnt 列表。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.set_axis_visibility(
    visible=True,
    hierarchy=True,
    all_jnts=[],
)
```

#### `create_snap_jnts()`

**作用**

在当前选择的 Object / Component 世界位置创建 Jnt。

**Signature**

```python
create_snap_jnts(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.create_snap_jnts()
```

#### `create_child_jnts()`

**作用**

为当前选择的 Transform / Jnt 创建同姿态 Child Jnt。

**Signature**

```python
create_child_jnts(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.create_child_jnts()
```

#### `open_resample_tool()`

**作用**

通过统一 Window Manager 打开 Jnt Resample。

**Signature**

```python
open_resample_tool()
```

**参数**

无。

**返回值**

object:
    当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.open_resample_tool()
```

#### `parent_selected_chain()`

**作用**

按照当前 Jnt Selection 顺序组成父子链。

**Signature**

```python
parent_selected_chain()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.parent_selected_chain()
```

#### `create_jnts_on_curves()`

**作用**

根据选择 Curve 的 CV 创建 Jnt Chain。

**Signature**

```python
create_jnts_on_curves(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.create_jnts_on_curves()
```

#### `create_jnts_on_edges()`

**作用**

把当前 Polygon Edge Selection 转 Curve 后创建 Jnt Chain。

**Signature**

```python
create_jnts_on_edges(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.create_jnts_on_edges()
```

#### `enable_scale_compensate()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
enable_scale_compensate(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.enable_scale_compensate()
```

#### `disable_scale_compensate()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
disable_scale_compensate(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.disable_scale_compensate()
```

#### `set_scale_compensate()`

**作用**

设置选择 Jnt 的 Segment Scale Compensate。

**Signature**

```python
set_scale_compensate(self, enabled)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `enabled` | `bool` | 是 | `—` | 当前 UI 控件或 Rig 功能是否启用。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.set_scale_compensate(
    enabled=True,
)
```

#### `show_orient()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
show_orient(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.show_orient()
```

#### `hide_orient()`

**作用**

执行当前 API 的主要处理流程。

**Signature**

```python
hide_orient(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.hide_orient()
```

#### `set_orient_visibility()`

**作用**

显示或隐藏选择 Jnt 的 jointOrient Channel。

**Signature**

```python
set_orient_visibility(self, visible)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `visible` | `bool` | 是 | `—` | Jnt / Guide / UI 元素是否保持可见。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.set_orient_visibility(
    visible=True,
)
```

#### `clear_jnt_orient()`

**作用**

清空选择 Jnt 的 jntOrientXYZ。

**Signature**

```python
clear_jnt_orient(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.clear_jnt_orient()
```

#### `create_curve_on_jnts()`

**作用**

按照选择 Jnt 世界位置创建 Curve。

**Signature**

```python
create_curve_on_jnts(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.create_curve_on_jnts()
```

#### `batch_parent_constraint()`

**作用**

按照 driver/driven 成对顺序批量创建 Parent Constraint。

**Signature**

```python
batch_parent_constraint()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.batch_parent_constraint()
```

#### `copy_skin_weights()`

**作用**

把第一个选择模型 Skin Weight 复制到后续选择模型。

**Signature**

```python
copy_skin_weights()
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.jnt import jnt_tool

instance = jnt_tool.JntTool()

result = instance.copy_skin_weights()
```

## 源码位置

`tools/jnt/jnt_tool.py`
