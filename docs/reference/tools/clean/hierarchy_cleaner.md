<!-- AUTO-GENERATED BY scripts/generate_mkdocs_reference.py -->

# `muziToolset.tools.clean.hierarchy_cleaner`

> 本页由 `scripts/generate_mkdocs_reference.py` 根据当前源码 AST 自动生成。
> 源码 Docstring 是参数、返回值、异常和示例说明的第一事实来源。

## 概览

**用途**

场景安全清理 UI。

**模块定位**

Tool 用户操作层；负责 UI、Selection 和调用底层能力。
## 常用任务

- 绑定师需要直接打开一个独立 Maya 小工具完成具体操作时。
- UI 负责收集 Selection / 参数，再调用 Core 或 Systems 时。
- 检查或清理 Maya 场景、模型和绑定层级时。

## Import

```python
from muziToolset.tools.clean import hierarchy_cleaner
```

## API 一览

### Functions

| API | 作用 |
| --- | --- |
| `main()` | 显示并返回 Hierarchy Cleaner。 |

### Class `HierarchyCleaner`

层级清理器窗口。

| Method | 作用 |
| --- | --- |
| `create_widgets(self)` | 创建界面控件。 |
| `create_layouts(self)` | 创建 Card 布局。 |
| `create_connections(self)` | 连接 UI 信号。 |
| `confirm_whole_scene(self)` | 全场景模式二次确认。 |
| `get_scope_nodes(self)` | 返回当前清理范围。 |
| `format_result(self, result)` | 把 Core 返回字典格式化成 UI 文本。 |
| `execute_cleanup(self)` | 执行当前配置的场景清理。 |

## Functions 详细 API

### `main()`

**作用**

显示并返回 Hierarchy Cleaner。

**Signature**

```python
main()
```

**参数**

无。

**返回值**

object:
    当前工具入口创建并显示的窗口或执行结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.clean import hierarchy_cleaner

result = hierarchy_cleaner.main()
```

## Classes 详细 API

### `HierarchyCleaner`

层级清理器窗口。

#### `__init__()`

**作用**

初始化当前对象，并准备运行时需要的状态和成员。

**Signature**

```python
__init__(self, parent=None)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `parent` | `str` | 否 | `None` | 父级 Maya 节点名称。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.clean import hierarchy_cleaner

instance = hierarchy_cleaner.HierarchyCleaner()
```

#### `create_widgets()`

**作用**

创建界面控件。

**Signature**

```python
create_widgets(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.clean import hierarchy_cleaner

instance = hierarchy_cleaner.HierarchyCleaner()

result = instance.create_widgets()
```

#### `create_layouts()`

**作用**

创建 Card 布局。

**Signature**

```python
create_layouts(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.clean import hierarchy_cleaner

instance = hierarchy_cleaner.HierarchyCleaner()

result = instance.create_layouts()
```

#### `create_connections()`

**作用**

连接 UI 信号。

**Signature**

```python
create_connections(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.clean import hierarchy_cleaner

instance = hierarchy_cleaner.HierarchyCleaner()

result = instance.create_connections()
```

#### `confirm_whole_scene()`

**作用**

全场景模式二次确认。

**Signature**

```python
confirm_whole_scene(self)
```

**参数**

无。

**返回值**

object | bool:
    当前操作成功或目标状态满足要求时返回 True，否则返回 False。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.clean import hierarchy_cleaner

instance = hierarchy_cleaner.HierarchyCleaner()

result = instance.confirm_whole_scene()
```

#### `get_scope_nodes()`

**作用**

返回当前清理范围。

**Signature**

```python
get_scope_nodes(self)
```

**参数**

无。

**返回值**

object:
    当前查询匹配到的 Maya / Rig 数据；没有结果时按 API 约定返回空值。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.clean import hierarchy_cleaner

instance = hierarchy_cleaner.HierarchyCleaner()

result = instance.get_scope_nodes()
```

#### `format_result()`

**作用**

把 Core 返回字典格式化成 UI 文本。

**Signature**

```python
format_result(self, result)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `result` | `object` | 是 | `—` | 上一步 Maya / Tool 操作返回的结果数据。 |

**返回值**

object:
    当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.clean import hierarchy_cleaner

instance = hierarchy_cleaner.HierarchyCleaner()

result = instance.format_result(
    result=...,
)
```

#### `execute_cleanup()`

**作用**

执行当前配置的场景清理。

**Signature**

```python
execute_cleanup(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.clean import hierarchy_cleaner

instance = hierarchy_cleaner.HierarchyCleaner()

result = instance.execute_cleanup()
```

## 源码位置

`tools/clean/hierarchy_cleaner.py`
