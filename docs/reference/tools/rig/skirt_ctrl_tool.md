<!-- AUTO-GENERATED BY scripts/generate_mkdocs_reference.py -->

# `muziToolset.tools.rig.skirt_ctrl_tool`

> 本页由 `scripts/generate_mkdocs_reference.py` 根据当前源码 AST 自动生成。
> 源码 Docstring 是参数、返回值、异常和示例说明的第一事实来源。

## 概览

**用途**

裙子绑定系统 UI。

**模块定位**

Tool 用户操作层；负责 UI、Selection 和调用底层能力。
## 常用任务

- 绑定师需要直接打开一个独立 Maya 小工具完成具体操作时。
- UI 负责收集 Selection / 参数，再调用 Core 或 Systems 时。

## Import

```python
from muziToolset.tools.rig import skirt_ctrl_tool
```

## API 一览

### Functions

| API | 作用 |
| --- | --- |
| `main()` | 显示并返回 Skirt Rig 窗口。 |

### Class `SkirtRigDialog`

裙子绑定工具窗口。

| Method | 作用 |
| --- | --- |
| `create_widgets(self)` | 创建界面控件。 |
| `create_layouts(self)` | 创建 Card 布局。 |
| `create_connections(self)` | 连接 UI 信号。 |
| `create_builder(self)` | 根据当前 UI 参数创建 Skirt Builder。 |
| `create_setup(self)` | 创建或重建定位系统。 |
| `select_setup_curves(self)` | 选择定位曲线。 |
| `build_rig(self)` | 执行完整裙子绑定 Build。 |

## Functions 详细 API

### `main()`

**作用**

显示并返回 Skirt Rig 窗口。

**Signature**

```python
main()
```

**参数**

无。

**返回值**

object:
    当前工具入口创建并显示的窗口或执行结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.rig import skirt_ctrl_tool

result = skirt_ctrl_tool.main()
```

## Classes 详细 API

### `SkirtRigDialog`

裙子绑定工具窗口。

#### `__init__()`

**作用**

初始化当前对象，并准备运行时需要的状态和成员。

**Signature**

```python
__init__(self, parent=None)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `parent` | `str` | 否 | `None` | 父级 Maya 节点名称。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.rig import skirt_ctrl_tool

instance = skirt_ctrl_tool.SkirtRigDialog()
```

#### `create_widgets()`

**作用**

创建界面控件。

**Signature**

```python
create_widgets(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.rig import skirt_ctrl_tool

instance = skirt_ctrl_tool.SkirtRigDialog()

result = instance.create_widgets()
```

#### `create_layouts()`

**作用**

创建 Card 布局。

**Signature**

```python
create_layouts(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.rig import skirt_ctrl_tool

instance = skirt_ctrl_tool.SkirtRigDialog()

result = instance.create_layouts()
```

#### `create_connections()`

**作用**

连接 UI 信号。

**Signature**

```python
create_connections(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.rig import skirt_ctrl_tool

instance = skirt_ctrl_tool.SkirtRigDialog()

result = instance.create_connections()
```

#### `create_builder()`

**作用**

根据当前 UI 参数创建 Skirt Builder。

**Signature**

```python
create_builder(self)
```

**参数**

无。

**返回值**

object:
    创建或构建完成后的 Maya / Rig 对象或 Build Result。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.rig import skirt_ctrl_tool

instance = skirt_ctrl_tool.SkirtRigDialog()

result = instance.create_builder()
```

#### `create_setup()`

**作用**

创建或重建定位系统。

**Signature**

```python
create_setup(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.rig import skirt_ctrl_tool

instance = skirt_ctrl_tool.SkirtRigDialog()

result = instance.create_setup()
```

#### `select_setup_curves()`

**作用**

选择定位曲线。

**Signature**

```python
select_setup_curves(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.rig import skirt_ctrl_tool

instance = skirt_ctrl_tool.SkirtRigDialog()

result = instance.select_setup_curves()
```

#### `build_rig()`

**作用**

执行完整裙子绑定 Build。

**Signature**

```python
build_rig(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.rig import skirt_ctrl_tool

instance = skirt_ctrl_tool.SkirtRigDialog()

result = instance.build_rig()
```

## 源码位置

`tools/rig/skirt_ctrl_tool.py`
