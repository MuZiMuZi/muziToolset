<!-- AUTO-GENERATED BY scripts/generate_mkdocs_reference.py -->

# `muziToolset.tools.basic.rename_tool`

> 本页由 `scripts/generate_mkdocs_reference.py` 根据当前源码 AST 自动生成。
> 源码 Docstring 是参数、返回值、异常和示例说明的第一事实来源。

## 概览

**用途**

批量重命名工具 UI。

**模块定位**

Tool 用户操作层；负责 UI、Selection 和调用底层能力。
## 常用任务

- 绑定师需要直接打开一个独立 Maya 小工具完成具体操作时。
- UI 负责收集 Selection / 参数，再调用 Core 或 Systems 时。
- 批量整理节点命名时。

## Import

```python
from muziToolset.tools.basic import rename_tool
```

## API 一览

### Functions

| API | 作用 |
| --- | --- |
| `main()` | 创建或恢复 Rename Tool，立即显示并返回 QWidget。 |

### Class `RenameTool`

批量重命名工具窗口。

| Method | 作用 |
| --- | --- |
| `create_widgets(self)` | 创建界面控件。 |
| `create_layouts(self)` | 创建 Card 布局。 |
| `create_connections(self)` | 连接 UI 信号。 |
| `set_result_status(self, action_name, count)` | 更新执行结果状态。 |
| `add_prefix(self)` | 读取 Prefix 输入并执行批量添加。 |
| `add_suffix(self)` | 读取 Suffix 输入并执行批量添加。 |
| `search_replace(self)` | 按照当前 Scope 执行 Search / Replace。 |
| `auto_number(self)` | 按照 Selection 顺序执行自动编号。 |
| `pattern_rename(self)` | 按照 * 占位规则执行 Pattern Rename。 |

## Functions 详细 API

### `main()`

**作用**

创建或恢复 Rename Tool，立即显示并返回 QWidget。

**Signature**

```python
main()
```

**参数**

无。

**返回值**

object:
    当前工具入口创建并显示的窗口或执行结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.basic import rename_tool

result = rename_tool.main()
```

## Classes 详细 API

### `RenameTool`

批量重命名工具窗口。

#### `__init__()`

**作用**

初始化当前对象，并准备运行时需要的状态和成员。

**Signature**

```python
__init__(self, parent=None)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `parent` | `str` | 否 | `None` | 父级 Maya 节点名称。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.basic import rename_tool

instance = rename_tool.RenameTool()
```

#### `create_widgets()`

**作用**

创建界面控件。

**Signature**

```python
create_widgets(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.basic import rename_tool

instance = rename_tool.RenameTool()

result = instance.create_widgets()
```

#### `create_layouts()`

**作用**

创建 Card 布局。

**Signature**

```python
create_layouts(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.basic import rename_tool

instance = rename_tool.RenameTool()

result = instance.create_layouts()
```

#### `create_connections()`

**作用**

连接 UI 信号。

**Signature**

```python
create_connections(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.basic import rename_tool

instance = rename_tool.RenameTool()

result = instance.create_connections()
```

#### `set_result_status()`

**作用**

更新执行结果状态。

**Signature**

```python
set_result_status(self, action_name, count)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `action_name` | `str` | 是 | `—` | `action_name` 对应的 Maya 节点或资源名称。 |
| `count` | `int` | 是 | `—` | 需要创建、采样或处理的数量。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.basic import rename_tool

instance = rename_tool.RenameTool()

result = instance.set_result_status(
    action_name="action_name",
    count=1,
)
```

#### `add_prefix()`

**作用**

读取 Prefix 输入并执行批量添加。

**Signature**

```python
add_prefix(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.basic import rename_tool

instance = rename_tool.RenameTool()

result = instance.add_prefix()
```

#### `add_suffix()`

**作用**

读取 Suffix 输入并执行批量添加。

**Signature**

```python
add_suffix(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.basic import rename_tool

instance = rename_tool.RenameTool()

result = instance.add_suffix()
```

#### `search_replace()`

**作用**

按照当前 Scope 执行 Search / Replace。

**Signature**

```python
search_replace(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.basic import rename_tool

instance = rename_tool.RenameTool()

result = instance.search_replace()
```

#### `auto_number()`

**作用**

按照 Selection 顺序执行自动编号。

**Signature**

```python
auto_number(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.basic import rename_tool

instance = rename_tool.RenameTool()

result = instance.auto_number()
```

#### `pattern_rename()`

**作用**

按照 * 占位规则执行 Pattern Rename。

**Signature**

```python
pattern_rename(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.basic import rename_tool

instance = rename_tool.RenameTool()

result = instance.pattern_rename()
```

## 源码位置

`tools/basic/rename_tool.py`
