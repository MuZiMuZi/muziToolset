<!-- AUTO-GENERATED BY scripts/generate_mkdocs_reference.py -->

# `muziToolset.tools.basic.constraint_tool`

> 本页由 `scripts/generate_mkdocs_reference.py` 根据当前源码 AST 自动生成。
> 源码 Docstring 是参数、返回值、异常和示例说明的第一事实来源。

## 概览

**用途**

Maya 常用约束工具窗口。

**模块定位**

Tool 用户操作层；负责 UI、Selection 和调用底层能力。
## 常用任务

- 绑定师需要直接打开一个独立 Maya 小工具完成具体操作时。
- UI 负责收集 Selection / 参数，再调用 Core 或 Systems 时。

## Import

```python
from muziToolset.tools.basic import constraint_tool
```

## API 一览

### Functions

| API | 作用 |
| --- | --- |
| `main()` | 创建或恢复 Constraint Tool，立即显示并返回 QWidget。 |

### Class `ConstraintTool`

Maya 常用约束工具窗口。

| Method | 作用 |
| --- | --- |
| `create_widgets(self)` | 创建窗口中使用的所有部件。 |
| `create_layouts(self)` | 创建 Card 布局。 |
| `create_connections(self)` | 连接所有按钮事件。 |
| `get_driver_and_driven_objects(self)` | 根据当前模式拆分 Driver / Driven。 |
| `create_standard_constraint(self, constraint_type, chunk_name)` | 收集 UI 参数并按 Driven 循环调用 Core 创建标准约束。 |
| `clicked_parent_constraint_button(self)` | 创建 Parent Constraint。 |
| `clicked_point_constraint_button(self)` | 创建 Point Constraint。 |
| `clicked_orient_constraint_button(self)` | 创建 Orient Constraint。 |
| `clicked_scale_constraint_button(self)` | 创建 Scale Constraint。 |
| `clicked_aim_constraint_button(self)` | 创建 Aim Constraint。 |
| `clicked_pole_vector_constraint_button(self)` | 创建 Pole Vector Constraint。 |
| `get_constraints_from_objects(selected_objects)` | 从 Core 查询真正驱动输入对象的 Constraint 节点。 |
| `clicked_select_constraint_button(self)` | 选择真正驱动当前选择对象的所有 Constraint 节点。 |
| `clicked_delete_constraint_button(self)` | 删除真正驱动当前选择对象的所有 Constraint 节点。 |

## Functions 详细 API

### `main()`

**作用**

创建或恢复 Constraint Tool，立即显示并返回 QWidget。

**Signature**

```python
main()
```

**参数**

无。

**返回值**

object:
    当前工具入口创建并显示的窗口或执行结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.basic import constraint_tool

result = constraint_tool.main()
```

## Classes 详细 API

### `ConstraintTool`

Maya 常用约束工具窗口。

#### `__init__()`

**作用**

初始化当前对象，并准备运行时需要的状态和成员。

**Signature**

```python
__init__(self, parent=None)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `parent` | `str` | 否 | `None` | 父级 Maya 节点名称。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.basic import constraint_tool

instance = constraint_tool.ConstraintTool()
```

#### `create_widgets()`

**作用**

创建窗口中使用的所有部件。

**Signature**

```python
create_widgets(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.basic import constraint_tool

instance = constraint_tool.ConstraintTool()

result = instance.create_widgets()
```

#### `create_layouts()`

**作用**

创建 Card 布局。

**Signature**

```python
create_layouts(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.basic import constraint_tool

instance = constraint_tool.ConstraintTool()

result = instance.create_layouts()
```

#### `create_connections()`

**作用**

连接所有按钮事件。

**Signature**

```python
create_connections(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.basic import constraint_tool

instance = constraint_tool.ConstraintTool()

result = instance.create_connections()
```

#### `get_driver_and_driven_objects()`

**作用**

根据当前模式拆分 Driver / Driven。

**Signature**

```python
get_driver_and_driven_objects(self)
```

**参数**

无。

**返回值**

tuple:
    按当前 API 约定组织的结果元组。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.basic import constraint_tool

instance = constraint_tool.ConstraintTool()

result = instance.get_driver_and_driven_objects()
```

#### `create_standard_constraint()`

**作用**

收集 UI 参数并按 Driven 循环调用 Core 创建标准约束。

**Signature**

```python
create_standard_constraint(self, constraint_type, chunk_name)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `constraint_type` | `str` | 是 | `—` | Maya Constraint 类型，例如 parentConstraint、pointConstraint、orientConstraint、scaleConstraint 或 aimConstraint。 |
| `chunk_name` | `str` | 是 | `—` | 当前按钮操作使用的 Maya Undo Chunk 名称。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.basic import constraint_tool

instance = constraint_tool.ConstraintTool()

result = instance.create_standard_constraint(
    constraint_type=...,
    chunk_name="chunk_name",
)
```

#### `clicked_parent_constraint_button()`

**作用**

创建 Parent Constraint。

**Signature**

```python
clicked_parent_constraint_button(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.basic import constraint_tool

instance = constraint_tool.ConstraintTool()

result = instance.clicked_parent_constraint_button()
```

#### `clicked_point_constraint_button()`

**作用**

创建 Point Constraint。

**Signature**

```python
clicked_point_constraint_button(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.basic import constraint_tool

instance = constraint_tool.ConstraintTool()

result = instance.clicked_point_constraint_button()
```

#### `clicked_orient_constraint_button()`

**作用**

创建 Orient Constraint。

**Signature**

```python
clicked_orient_constraint_button(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.basic import constraint_tool

instance = constraint_tool.ConstraintTool()

result = instance.clicked_orient_constraint_button()
```

#### `clicked_scale_constraint_button()`

**作用**

创建 Scale Constraint。

**Signature**

```python
clicked_scale_constraint_button(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.basic import constraint_tool

instance = constraint_tool.ConstraintTool()

result = instance.clicked_scale_constraint_button()
```

#### `clicked_aim_constraint_button()`

**作用**

创建 Aim Constraint。

**Signature**

```python
clicked_aim_constraint_button(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.basic import constraint_tool

instance = constraint_tool.ConstraintTool()

result = instance.clicked_aim_constraint_button()
```

#### `clicked_pole_vector_constraint_button()`

**作用**

创建 Pole Vector Constraint。

**Signature**

```python
clicked_pole_vector_constraint_button(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.basic import constraint_tool

instance = constraint_tool.ConstraintTool()

result = instance.clicked_pole_vector_constraint_button()
```

#### `get_constraints_from_objects()`

**作用**

从 Core 查询真正驱动输入对象的 Constraint 节点。

**Signature**

```python
get_constraints_from_objects(selected_objects)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `selected_objects` | `str \| list[str]` | 是 | `—` | 需要作为 Driven 查询 Constraint 的 Maya 节点。 |

**返回值**

list[str]:
    真正向这些对象输出驱动结果的 Constraint 节点。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.basic import constraint_tool

instance = constraint_tool.ConstraintTool()

result = instance.get_constraints_from_objects(
    selected_objects=[],
)
```

#### `clicked_select_constraint_button()`

**作用**

选择真正驱动当前选择对象的所有 Constraint 节点。

**Signature**

```python
clicked_select_constraint_button(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.basic import constraint_tool

instance = constraint_tool.ConstraintTool()

result = instance.clicked_select_constraint_button()
```

#### `clicked_delete_constraint_button()`

**作用**

删除真正驱动当前选择对象的所有 Constraint 节点。

**Signature**

```python
clicked_delete_constraint_button(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.basic import constraint_tool

instance = constraint_tool.ConstraintTool()

result = instance.clicked_delete_constraint_button()
```

## 源码位置

`tools/basic/constraint_tool.py`
