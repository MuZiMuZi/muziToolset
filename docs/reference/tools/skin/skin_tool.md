<!-- AUTO-GENERATED BY scripts/generate_mkdocs_reference.py -->

# `muziToolset.tools.skin.skin_tool`

> 本页由 `scripts/generate_mkdocs_reference.py` 根据当前源码 AST 自动生成。
> 源码 Docstring 是参数、返回值、异常和示例说明的第一事实来源。

## 概览

**用途**

蒙皮工具 UI。

**模块定位**

Tool 用户操作层；负责 UI、Selection 和调用底层能力。
## 常用任务

- 绑定师需要直接打开一个独立 Maya 小工具完成具体操作时。
- UI 负责收集 Selection / 参数，再调用 Core 或 Systems 时。
- 创建 SkinCluster、处理权重或绑定关系时。

## Import

```python
from muziToolset.tools.skin import skin_tool
```

## API 一览

### Functions

| API | 作用 |
| --- | --- |
| `main()` | 显示并返回 Skin Tool。 |

### Class `SkinTool`

蒙皮工具窗口。

| Method | 作用 |
| --- | --- |
| `create_widgets(self)` | 创建界面控件。 |
| `create_layouts(self)` | 创建 Card 布局。 |
| `create_connections(self)` | 连接 UI 信号。 |
| `open_smooth_bind_options(self)` | 打开当前 smooth bind options。 |
| `open_detach_skin_options(self)` | 打开当前 detach skin options。 |
| `open_paint_skin_weights(self)` | 打开当前 paint skin weights。 |
| `open_mirror_skin_weights_options(self)` | 打开当前 mirror skin weights options。 |
| `get_selected_geometries(self)` | 返回当前选择。 |
| `copy_selected(self)` | 复制当前选择的 Skin Weight。 |
| `choose_directory(self, title)` | 选择权重目录。 |
| `export_selected(self)` | 导出选择模型权重。 |
| `import_selected(self)` | 从目录导入权重到选择模型。 |
| `select_influences(self)` | 选择当前模型的影响 Jnt。 |
| `normalize_selected(self)` | 强制归一化选择模型权重。 |

## Functions 详细 API

### `main()`

**作用**

显示并返回 Skin Tool。

**Signature**

```python
main()
```

**参数**

无。

**返回值**

object:
    当前工具入口创建并显示的窗口或执行结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.skin import skin_tool

result = skin_tool.main()
```

## Classes 详细 API

### `SkinTool`

蒙皮工具窗口。

#### `__init__()`

**作用**

初始化当前对象，并准备运行时需要的状态和成员。

**Signature**

```python
__init__(self, parent=None)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `parent` | `str` | 否 | `None` | 父级 Maya 节点名称。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.skin import skin_tool

instance = skin_tool.SkinTool()
```

#### `create_widgets()`

**作用**

创建界面控件。

**Signature**

```python
create_widgets(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.skin import skin_tool

instance = skin_tool.SkinTool()

result = instance.create_widgets()
```

#### `create_layouts()`

**作用**

创建 Card 布局。

**Signature**

```python
create_layouts(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.skin import skin_tool

instance = skin_tool.SkinTool()

result = instance.create_layouts()
```

#### `create_connections()`

**作用**

连接 UI 信号。

**Signature**

```python
create_connections(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.skin import skin_tool

instance = skin_tool.SkinTool()

result = instance.create_connections()
```

#### `open_smooth_bind_options()`

**作用**

打开当前 smooth bind options。

**Signature**

```python
open_smooth_bind_options(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.skin import skin_tool

instance = skin_tool.SkinTool()

result = instance.open_smooth_bind_options()
```

#### `open_detach_skin_options()`

**作用**

打开当前 detach skin options。

**Signature**

```python
open_detach_skin_options(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.skin import skin_tool

instance = skin_tool.SkinTool()

result = instance.open_detach_skin_options()
```

#### `open_paint_skin_weights()`

**作用**

打开当前 paint skin weights。

**Signature**

```python
open_paint_skin_weights(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.skin import skin_tool

instance = skin_tool.SkinTool()

result = instance.open_paint_skin_weights()
```

#### `open_mirror_skin_weights_options()`

**作用**

打开当前 mirror skin weights options。

**Signature**

```python
open_mirror_skin_weights_options(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.skin import skin_tool

instance = skin_tool.SkinTool()

result = instance.open_mirror_skin_weights_options()
```

#### `get_selected_geometries()`

**作用**

返回当前选择。

**Signature**

```python
get_selected_geometries(self)
```

**参数**

无。

**返回值**

object:
    当前查询匹配到的 Maya / Rig 数据；没有结果时按 API 约定返回空值。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.skin import skin_tool

instance = skin_tool.SkinTool()

result = instance.get_selected_geometries()
```

#### `copy_selected()`

**作用**

复制当前选择的 Skin Weight。

**Signature**

```python
copy_selected(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.skin import skin_tool

instance = skin_tool.SkinTool()

result = instance.copy_selected()
```

#### `choose_directory()`

**作用**

选择权重目录。

**Signature**

```python
choose_directory(self, title)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `title` | `str` | 是 | `—` | 窗口、Section、Dialog 或报告使用的标题文本。 |

**返回值**

object:
    当前 API 完成处理后返回的结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.skin import skin_tool

instance = skin_tool.SkinTool()

result = instance.choose_directory(
    title=...,
)
```

#### `export_selected()`

**作用**

导出选择模型权重。

**Signature**

```python
export_selected(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.skin import skin_tool

instance = skin_tool.SkinTool()

result = instance.export_selected()
```

#### `import_selected()`

**作用**

从目录导入权重到选择模型。

**Signature**

```python
import_selected(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.skin import skin_tool

instance = skin_tool.SkinTool()

result = instance.import_selected()
```

#### `select_influences()`

**作用**

选择当前模型的影响 Jnt。

**Signature**

```python
select_influences(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.skin import skin_tool

instance = skin_tool.SkinTool()

result = instance.select_influences()
```

#### `normalize_selected()`

**作用**

强制归一化选择模型权重。

**Signature**

```python
normalize_selected(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.skin import skin_tool

instance = skin_tool.SkinTool()

result = instance.normalize_selected()
```

## 源码位置

`tools/skin/skin_tool.py`
