<!-- AUTO-GENERATED BY scripts/generate_mkdocs_reference.py -->

# `muziToolset.tools.face.face_rig_tool`

> 本页由 `scripts/generate_mkdocs_reference.py` 根据当前源码 AST 自动生成。
> 源码 Docstring 是参数、返回值、异常和示例说明的第一事实来源。

## 概览

**用途**

面部工具分类中的 Face Rig 系统启动入口。

**模块定位**

Tool 用户操作层；负责 UI、Selection 和调用底层能力。
## 常用任务

- 绑定师需要直接打开一个独立 Maya 小工具完成具体操作时。
- UI 负责收集 Selection / 参数，再调用 Core 或 Systems 时。
- 构建或管理 Face Setup、Guide、Eyelid、Lip 等面部绑定流程时。

## Import

```python
from muziToolset.tools.face import face_rig_tool
```

## API 一览

### Functions

| API | 作用 |
| --- | --- |
| `main()` | 显示并返回 Face Rig Wizard。 |

## Functions 详细 API

### `main()`

**作用**

显示并返回 Face Rig Wizard。

**Signature**

```python
main()
```

**参数**

无。

**返回值**

object:
    当前工具入口创建并显示的窗口或执行结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.face import face_rig_tool

result = face_rig_tool.main()
```

## 源码位置

`tools/face/face_rig_tool.py`
