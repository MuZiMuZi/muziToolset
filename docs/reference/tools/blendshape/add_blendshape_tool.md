<!-- AUTO-GENERATED BY scripts/generate_mkdocs_reference.py -->

# `muziToolset.tools.blendshape.add_blendshape_tool`

> 本页由 `scripts/generate_mkdocs_reference.py` 根据当前源码 AST 自动生成。
> 源码 Docstring 是参数、返回值、异常和示例说明的第一事实来源。

## 概览

**用途**

BlendShape Target 管理 UI。

**模块定位**

Tool 用户操作层；负责 UI、Selection 和调用底层能力。
## 常用任务

- 绑定师需要直接打开一个独立 Maya 小工具完成具体操作时。
- UI 负责收集 Selection / 参数，再调用 Core 或 Systems 时。
- 创建、编辑或查询 BlendShape / 修型目标时。

## Import

```python
from muziToolset.tools.blendshape import add_blendshape_tool
```

## API 一览

### Functions

| API | 作用 |
| --- | --- |
| `main()` | 显示并返回 BlendShape Target Tool。 |

### Class `BlendShapeTargetTool`

BlendShape Target 管理窗口。

| Method | 作用 |
| --- | --- |
| `create_widgets(self)` | 创建界面控件。 |
| `create_layouts(self)` | 创建 Card 布局。 |
| `create_connections(self)` | 连接 UI 信号。 |
| `get_blendshape_node(self)` | 返回当前输入的 BlendShape 节点。 |
| `pick_blendshape(self)` | 从当前选择查找 BlendShape。 |
| `refresh_targets(self)` | 刷新真实 Target Index。 |
| `add_targets(self)` | 把当前选择 Mesh 添加到 BlendShape。 |
| `duplicate_targets(self)` | 从 Base Mesh 烘焙全部 Target Mesh。 |

## Functions 详细 API

### `main()`

**作用**

显示并返回 BlendShape Target Tool。

**Signature**

```python
main()
```

**参数**

无。

**返回值**

object:
    当前工具入口创建并显示的窗口或执行结果。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.blendshape import add_blendshape_tool

result = add_blendshape_tool.main()
```

## Classes 详细 API

### `BlendShapeTargetTool`

BlendShape Target 管理窗口。

#### `__init__()`

**作用**

初始化当前对象，并准备运行时需要的状态和成员。

**Signature**

```python
__init__(self, parent=None)
```

**参数**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
| --- | --- | :---: | --- | --- |
| `parent` | `str` | 否 | `None` | 父级 Maya 节点名称。 |

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.blendshape import add_blendshape_tool

instance = add_blendshape_tool.BlendShapeTargetTool()
```

#### `create_widgets()`

**作用**

创建界面控件。

**Signature**

```python
create_widgets(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.blendshape import add_blendshape_tool

instance = add_blendshape_tool.BlendShapeTargetTool()

result = instance.create_widgets()
```

#### `create_layouts()`

**作用**

创建 Card 布局。

**Signature**

```python
create_layouts(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.blendshape import add_blendshape_tool

instance = add_blendshape_tool.BlendShapeTargetTool()

result = instance.create_layouts()
```

#### `create_connections()`

**作用**

连接 UI 信号。

**Signature**

```python
create_connections(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.blendshape import add_blendshape_tool

instance = add_blendshape_tool.BlendShapeTargetTool()

result = instance.create_connections()
```

#### `get_blendshape_node()`

**作用**

返回当前输入的 BlendShape 节点。

**Signature**

```python
get_blendshape_node(self)
```

**参数**

无。

**返回值**

object:
    当前查询匹配到的 Maya / Rig 数据；没有结果时按 API 约定返回空值。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.blendshape import add_blendshape_tool

instance = add_blendshape_tool.BlendShapeTargetTool()

result = instance.get_blendshape_node()
```

#### `pick_blendshape()`

**作用**

从当前选择查找 BlendShape。

**Signature**

```python
pick_blendshape(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.blendshape import add_blendshape_tool

instance = add_blendshape_tool.BlendShapeTargetTool()

result = instance.pick_blendshape()
```

#### `refresh_targets()`

**作用**

刷新真实 Target Index。

**Signature**

```python
refresh_targets(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.blendshape import add_blendshape_tool

instance = add_blendshape_tool.BlendShapeTargetTool()

result = instance.refresh_targets()
```

#### `add_targets()`

**作用**

把当前选择 Mesh 添加到 BlendShape。

**Signature**

```python
add_targets(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.blendshape import add_blendshape_tool

instance = add_blendshape_tool.BlendShapeTargetTool()

result = instance.add_targets()
```

#### `duplicate_targets()`

**作用**

从 Base Mesh 烘焙全部 Target Mesh。

**Signature**

```python
duplicate_targets(self)
```

**参数**

无。

**返回值**

源码未声明返回值说明。

**异常**

源码未声明专门的异常说明。

**示例**

```python
from muziToolset.tools.blendshape import add_blendshape_tool

instance = add_blendshape_tool.BlendShapeTargetTool()

result = instance.duplicate_targets()
```

## 源码位置

`tools/blendshape/add_blendshape_tool.py`
