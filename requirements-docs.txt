# MuziTools 文档构建依赖
#
# 安装：
#     python -m pip install -r requirements-docs.txt
#
# API Reference 由 scripts/generate_mkdocs_reference.py 使用 Python AST
# 静态读取源码，因此文档环境不需要 Maya，也不会 import maya.cmds。
#
# mkdocs-literate-nav 负责读取生成器输出的 docs/SUMMARY.md，
# 把每一个正式 Python 文件真正展示到左侧文档导航中。

mkdocs>=1.5,<2.0
mkdocs-material>=9.5,<10.0
mkdocs-literate-nav==0.6.3
